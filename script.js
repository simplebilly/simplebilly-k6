/*
 * SimpleBilly API
 * Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
 *
 * OpenAPI spec version: 0.1.0
 * Contact: support@simplebilly.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 * https://github.com/OpenAPITools/openapi-generator
 *
 * Generator version: 7.25.0
 */


import http from "k6/http";
import { group, check, sleep } from "k6";

const BASE_URL = "https://{subdomain}.simplebilly.com";
// Sleep duration between successive requests.
// You might want to edit the value of this variable or remove calls to the sleep function on the script.
const SLEEP_DURATION = 0.1;
// Global variables should be initialized.

export default function() {
    group("/api/v1/bookkeeping/gewinnverwendung", () => {
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: gewinnverwendung_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/gewinnverwendung?year=${year}`;
            let request = http.get(url);

            check(request, {
                "Gewinnverwendungsrechnung (§ 150, § 174 AktG)": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/gdpr/account-erasure", () => {

        // Request No. 1: account_erasure
        {
            let url = BASE_URL + `/api/v1/gdpr/account-erasure`;
            let request = http.post(url);

            check(request, {
                "Tenant personal data anonymized": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/order-confirmations/{confirmation_id}/restore", () => {
        let confirmationId = 'TODO_EDIT_THE_CONFIRMATION_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: orderconfirmation_restore
        {
            let url = BASE_URL + `/api/v1/order-confirmations/${confirmation_id}/restore`;
            let request = http.post(url);

            check(request, {
                "Restored": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/propose-assignments", () => {
        let minConfidence = 'TODO_EDIT_THE_MIN_CONFIDENCE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let customerId = 'TODO_EDIT_THE_CUSTOMER_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: propose_assignments_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/propose-assignments?min_confidence=${min_confidence}&customer_id=${customer_id}`;
            let request = http.get(url);

            check(request, {
                "Proposed payment to invoice assignments": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/webhook-subscriptions", () => {

        // Request No. 1: list_subscriptions
        {
            let url = BASE_URL + `/api/v1/webhook-subscriptions`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: create_subscription
        {
            let url = BASE_URL + `/api/v1/webhook-subscriptions`;
            // TODO: edit the parameters of the request body.
            let body = {"eventType": "string", "isActive": "boolean", "name": "string", "secret": "string", "url": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/pos/orders/{order_number}/receipt", () => {
        let orderNumber = 'TODO_EDIT_THE_ORDER_NUMBER'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: pos_order_receipt
        {
            let url = BASE_URL + `/api/pos/orders/${order_number}/receipt`;
            let request = http.get(url);

            check(request, {
                "": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/marketplace/connections/{connection_id}", () => {
        let connectionId = 'TODO_EDIT_THE_CONNECTION_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_connection_api
        {
            let url = BASE_URL + `/api/v1/marketplace/connections/${connection_id}`;
            let request = http.get(url);

            check(request, {
                "Connection details": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_connection_api
        {
            let url = BASE_URL + `/api/v1/marketplace/connections/${connection_id}`;
            // TODO: edit the parameters of the request body.
            let body = {"apiKey": "string", "apiSecret": "string", "config": "oas_any_type_not_mapped", "isActive": "boolean", "label": "string", "shopDomain": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "Updated": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_connection_api
        {
            let url = BASE_URL + `/api/v1/marketplace/connections/${connection_id}`;
            let request = http.del(url);

            check(request, {
                "Deleted": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/shipping-thresholds/{threshold_id}", () => {
        let thresholdId = 'TODO_EDIT_THE_THRESHOLD_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_shipping_threshold
        {
            let url = BASE_URL + `/api/v1/shipping-thresholds/${threshold_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_shipping_threshold
        {
            let url = BASE_URL + `/api/v1/shipping-thresholds/${threshold_id}`;
            // TODO: edit the parameters of the request body.
            let body = {"isActive": "boolean", "maxSellable": "long", "name": "string", "notes": "string", "productId": "uuid", "reserveStock": "long", "warehouseId": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_shipping_threshold
        {
            let url = BASE_URL + `/api/v1/shipping-thresholds/${threshold_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/auth/magic-link/verify", () => {

        // Request No. 1: magic_link_verify
        {
            let url = BASE_URL + `/auth/magic-link/verify`;
            // TODO: edit the parameters of the request body.
            let body = {"token": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Magic link verified, returns JWT tokens": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/boms", () => {

        // Request No. 1: create_bom
        {
            let url = BASE_URL + `/api/v1/boms`;
            // TODO: edit the parameters of the request body.
            let body = {"components": "oas_any_type_not_mapped", "description": "string", "name": "string", "outputQuantity": "long", "productId": "uuid", "status": "bomstatus"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/stock-transfers/{stock_transfer_id}/status", () => {
        let stockTransferId = 'TODO_EDIT_THE_STOCK_TRANSFER_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: update_stock_transfer_status
        {
            let url = BASE_URL + `/api/v1/stock-transfers/${stock_transfer_id}/status`;
            // TODO: edit the parameters of the request body.
            let body = {"status": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/job-applications/{application_id}", () => {
        let applicationId = 'TODO_EDIT_THE_APPLICATION_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_job_application
        {
            let url = BASE_URL + `/api/v1/job-applications/${application_id}`;
            let request = http.get(url);

            check(request, {
                "Application": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: delete_job_application
        {
            let url = BASE_URL + `/api/v1/job-applications/${application_id}`;
            let request = http.del(url);

            check(request, {
                "Soft-deleted": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/delivery-dates/{delivery_date_id}", () => {
        let deliveryDateId = 'TODO_EDIT_THE_DELIVERY_DATE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_delivery_date
        {
            let url = BASE_URL + `/api/v1/delivery-dates/${delivery_date_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_delivery_date
        {
            let url = BASE_URL + `/api/v1/delivery-dates/${delivery_date_id}`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_delivery_date
        {
            let url = BASE_URL + `/api/v1/delivery-dates/${delivery_date_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/bookkeeping/stille/export", () => {
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: stille_export_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/stille/export?year=${year}`;
            let request = http.get(url);

            check(request, {
                "Stille Beteiligungen als CSV (BOM, Semikolon)": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/anlage-s", () => {
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: anlage_s_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/anlage-s?year=${year}`;
            let request = http.get(url);

            check(request, {
                "Anlage S Ergebnis": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/job-postings/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_job_posting
        {
            let url = BASE_URL + `/api/v1/job-postings/${id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_job_posting
        {
            let url = BASE_URL + `/api/v1/job-postings/${id}`;
            // TODO: edit the parameters of the request body.
            let body = {"currency": "string", "department": "string", "description": "string", "employmentType": "employmenttype", "location": "string", "remote": "boolean", "requiredSkills": "oas_any_type_not_mapped", "requirements": "string", "salaryMax": "integer", "salaryMin": "integer", "status": "jobpostingstatus", "title": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_job_posting
        {
            let url = BASE_URL + `/api/v1/job-postings/${id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/notifications/read-all", () => {

        // Request No. 1: mark_all_read
        {
            let url = BASE_URL + `/api/v1/notifications/read-all`;
            let request = http.put(url);

            check(request, {
                "All own notifications marked as read": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/users/{user_id}", () => {
        let userId = 'TODO_EDIT_THE_USER_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_user
        {
            let url = BASE_URL + `/api/v1/users/${user_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: remove_user
        {
            let url = BASE_URL + `/api/v1/users/${user_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/bookkeeping/konzern/status/export", () => {
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: konzern_export_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/konzern/status/export?year=${year}`;
            let request = http.get(url);

            check(request, {
                "Konzern-Beteiligungen mit Kontrollstatus als CSV (BOM, Semikolon)": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/coupons/{coupon_id}", () => {
        let couponId = 'TODO_EDIT_THE_COUPON_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_coupon
        {
            let url = BASE_URL + `/api/v1/coupons/${coupon_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_coupon
        {
            let url = BASE_URL + `/api/v1/coupons/${coupon_id}`;
            // TODO: edit the parameters of the request body.
            let body = {"code": "string", "description": "string", "discountType": "discounttype", "discountValue": "string", "expiresAt": "date", "isActive": "boolean", "isCombineable": "boolean", "maxDiscountAmount": "string", "maxUses": "integer", "maxUsesPerCustomer": "integer", "minOrderAmount": "string", "productIds": "oas_any_type_not_mapped", "startsAt": "date"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_coupon
        {
            let url = BASE_URL + `/api/v1/coupons/${coupon_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/invoices/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_invoice
        {
            let url = BASE_URL + `/api/v1/invoices/${id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_invoice
        {
            let url = BASE_URL + `/api/v1/invoices/${id}`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_invoice
        {
            let url = BASE_URL + `/api/v1/invoices/${id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/returns/logistics-queue", () => {

        // Request No. 1: return_logistics_queue
        {
            let url = BASE_URL + `/api/v1/returns/logistics-queue`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/production-orders/{production_order_id}/status", () => {
        let productionOrderId = 'TODO_EDIT_THE_PRODUCTION_ORDER_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: update_production_order_status
        {
            let url = BASE_URL + `/api/v1/production-orders/${production_order_id}/status`;
            // TODO: edit the parameters of the request body.
            let body = {"status": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/gdpr/dpa", () => {

        // Request No. 1: get_dpa
        {
            let url = BASE_URL + `/api/v1/gdpr/dpa`;
            let request = http.get(url);

            check(request, {
                "DPA acceptance status": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: accept_dpa
        {
            let url = BASE_URL + `/api/v1/gdpr/dpa`;
            // TODO: edit the parameters of the request body.
            let body = {"acceptedByName": "string", "version": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "DPA acceptance recorded": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/public/delivery-appointments/request", () => {

        // Request No. 1: request_public_delivery_appointment
        {
            let url = BASE_URL + `/api/v1/public/delivery-appointments/request`;
            // TODO: edit the parameters of the request body.
            let body = {"email": "string", "notes": "string", "requestedDate": "date", "supplierName": "string", "timeSlot": "string", "warehouseCode": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Appointment requested": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/public/track", () => {

        // Request No. 1: track_order_public
        {
            let url = BASE_URL + `/api/v1/public/track`;
            // TODO: edit the parameters of the request body.
            let body = {"email": "string", "orderNumber": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Order tracking info": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/trainings/me", () => {

        // Request No. 1: get_my_trainings
        {
            let url = BASE_URL + `/api/v1/trainings/me`;
            let request = http.get(url);

            check(request, {
                "Required trainings for the current employee": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/public/returns/list", () => {
        let orderNumber = 'TODO_EDIT_THE_ORDERNUMBER'; // specify value as there is no example value for this parameter in OpenAPI spec
        let email = 'TODO_EDIT_THE_EMAIL'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_public_returns
        {
            let url = BASE_URL + `/api/v1/public/returns/list?orderNumber=${orderNumber}&email=${email}`;
            let request = http.get(url);

            check(request, {
                "Returns for the order": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/anlage-g", () => {
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: anlage_g_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/anlage-g?year=${year}`;
            let request = http.get(url);

            check(request, {
                "Anlage G Ergebnis": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/service-assignments", () => {

        // Request No. 1: create_service_assignment
        {
            let url = BASE_URL + `/api/v1/service-assignments`;
            // TODO: edit the parameters of the request body.
            let body = {"employeeId": "uuid", "jobId": "uuid", "notes": "string", "scheduledDate": "date", "scheduledEnd": "string", "scheduledStart": "string", "status": "serviceassignmentstatus"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/customer-groups/", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let includeDeleted = 'TODO_EDIT_THE_INCLUDE_DELETED'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = '1'; // extracted from 'example' field defined at the parameter level of OpenAPI spec

        // Request No. 1: list_customer_groups
        {
            let url = BASE_URL + `/api/v1/customer-groups/?page=${page}&page_size=${page_size}&search=${search}&include_deleted=${include_deleted}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/email-templates/", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGESIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let status = 'TODO_EDIT_THE_STATUS'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_email_templates
        {
            let url = BASE_URL + `/api/v1/email-templates/?page=${page}&pageSize=${pageSize}&status=${status}&search=${search}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/payments/allocate", () => {

        // Request No. 1: allocate_payment_api
        {
            let url = BASE_URL + `/api/v1/payments/allocate`;
            // TODO: edit the parameters of the request body.
            let body = {"amount": "double", "invoiceId": "string", "paymentId": "uuid"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Payment allocated successfully": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/supplier-invoices/{supplier_invoice_id}/status", () => {
        let supplierInvoiceId = 'TODO_EDIT_THE_SUPPLIER_INVOICE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: update_supplier_invoice_status
        {
            let url = BASE_URL + `/api/v1/supplier-invoices/${supplier_invoice_id}/status`;
            // TODO: edit the parameters of the request body.
            let body = {"status": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/vouchers/{voucher_id}", () => {
        let voucherId = 'TODO_EDIT_THE_VOUCHER_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_voucher
        {
            let url = BASE_URL + `/api/v1/vouchers/${voucher_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_voucher
        {
            let url = BASE_URL + `/api/v1/vouchers/${voucher_id}`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_voucher
        {
            let url = BASE_URL + `/api/v1/vouchers/${voucher_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/automations/{key}", () => {
        let key = 'TODO_EDIT_THE_KEY'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: update_automation
        {
            let url = BASE_URL + `/api/v1/automations/${key}`;
            // TODO: edit the parameters of the request body.
            let body = {"config": "oas_any_type_not_mapped", "enabled": "boolean"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/service-jobs/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_service_job
        {
            let url = BASE_URL + `/api/v1/service-jobs/${id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_service_job
        {
            let url = BASE_URL + `/api/v1/service-jobs/${id}`;
            // TODO: edit the parameters of the request body.
            let body = {"address": "string", "customerEmail": "string", "customerId": "uuid", "customerName": "string", "customerPhone": "string", "description": "string", "estimatedDurationMinutes": "integer", "lat": "double", "lng": "double", "notes": "string", "status": "servicejobstatus"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_service_job
        {
            let url = BASE_URL + `/api/v1/service-jobs/${id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/product-attributes/", () => {
        let isFilterable = 'TODO_EDIT_THE_IS_FILTERABLE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let productId = 'TODO_EDIT_THE_PRODUCT_ID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_product_attributes
        {
            let url = BASE_URL + `/api/v1/product-attributes/?page=${page}&page_size=${page_size}&product_id=${product_id}&is_filterable=${is_filterable}&search=${search}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/absences/", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let includeDeleted = 'TODO_EDIT_THE_INCLUDE_DELETED'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = '1'; // extracted from 'example' field defined at the parameter level of OpenAPI spec

        // Request No. 1: get_absences
        {
            let url = BASE_URL + `/api/v1/absences/?page=${page}&page_size=${page_size}&search=${search}&include_deleted=${include_deleted}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/support/tickets/{ticket_id}/messages", () => {
        let ticketId = 'TODO_EDIT_THE_TICKET_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_messages_api
        {
            let url = BASE_URL + `/api/v1/support/tickets/${ticket_id}/messages`;
            let request = http.get(url);

            check(request, {
                "Messages for a ticket": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: send_message_api
        {
            let url = BASE_URL + `/api/v1/support/tickets/${ticket_id}/messages`;
            // TODO: edit the parameters of the request body.
            let body = {"body": "string", "isInternal": "boolean"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Message sent": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/bookkeeping/banking/lookup", () => {
        let iban = 'TODO_EDIT_THE_IBAN'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: bank_lookup_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/banking/lookup?iban=${iban}`;
            let request = http.get(url);

            check(request, {
                "Bank-Lookup Ergebnis": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/emissions/export", () => {
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: emissions_export_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/emissions/export?year=${year}`;
            let request = http.get(url);

            check(request, {
                "Emissions CSV export": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/webhooks/emit", () => {

        // Request No. 1: emit_api
        {
            let url = BASE_URL + `/api/v1/webhooks/emit`;
            // TODO: edit the parameters of the request body.
            let body = {"eventType": "string", "payload": "oas_any_type_not_mapped"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Emitted": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/budgets", () => {
        let month = 'TODO_EDIT_THE_MONTH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: budgets_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/budgets?year=${year}&month=${month}`;
            let request = http.get(url);

            check(request, {
                "Monats-Budget + Prognose": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/dunning", () => {

        // Request No. 1: run_dunning_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/dunning`;
            let request = http.post(url);

            check(request, {
                "Dunning run completed successfully": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/plans", () => {

        // Request No. 1: get_plans
        {
            let url = BASE_URL + `/api/v1/plans`;
            let request = http.get(url);

            check(request, {
                "All subscription plans": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/quotations/", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let includeDeleted = 'TODO_EDIT_THE_INCLUDE_DELETED'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = '1'; // extracted from 'example' field defined at the parameter level of OpenAPI spec

        // Request No. 1: list_quotations
        {
            let url = BASE_URL + `/api/v1/quotations/?page=${page}&page_size=${page_size}&search=${search}&include_deleted=${include_deleted}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/customers/{customer_id}/restore", () => {
        let customerId = 'TODO_EDIT_THE_CUSTOMER_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: customer_restore
        {
            let url = BASE_URL + `/api/v1/customers/${customer_id}/restore`;
            let request = http.post(url);

            check(request, {
                "Restored": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/products/{product_id}/stock", () => {
        let productId = 'TODO_EDIT_THE_PRODUCT_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_product_stock_api
        {
            let url = BASE_URL + `/api/v1/products/${product_id}/stock`;
            let request = http.get(url);

            check(request, {
                "Stock info": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_product_stock_api
        {
            let url = BASE_URL + `/api/v1/products/${product_id}/stock`;
            // TODO: edit the parameters of the request body.
            let body = {"quantity": "long"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "Stock updated": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/payroll/{id}/calculate", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: payroll_calculate
        {
            let url = BASE_URL + `/api/v1/payroll/${id}/calculate`;
            let request = http.post(url);

            check(request, {
                "": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/payment-gateways", () => {

        // Request No. 1: create_payment_gateway_api
        {
            let url = BASE_URL + `/api/v1/payment-gateways`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/contacts/schema", () => {

        // Request No. 1: contact_schema
        {
            let url = BASE_URL + `/api/v1/contacts/schema`;
            let request = http.get(url);

            check(request, {
                "JSON Schema for Contact": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/legal/documents", () => {

        // Request No. 1: get_legal_documents
        {
            let url = BASE_URL + `/api/v1/legal/documents`;
            let request = http.get(url);

            check(request, {
                "All legal documents of the tenant": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: upsert_legal_documents
        {
            let url = BASE_URL + `/api/v1/legal/documents`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, params);

            check(request, {
                "Saved legal documents": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/price-tiers/{price_tier_id}", () => {
        let priceTierId = 'TODO_EDIT_THE_PRICE_TIER_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_price_tier
        {
            let url = BASE_URL + `/api/v1/price-tiers/${price_tier_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_price_tier
        {
            let url = BASE_URL + `/api/v1/price-tiers/${price_tier_id}`;
            // TODO: edit the parameters of the request body.
            let body = {"customerGroupId": "string", "minQuantity": "long", "productId": "uuid", "unitPrice": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_price_tier
        {
            let url = BASE_URL + `/api/v1/price-tiers/${price_tier_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/quota", () => {

        // Request No. 1: get_quota_api
        {
            let url = BASE_URL + `/api/v1/quota`;
            let request = http.get(url);

            check(request, {
                "Effective limits and current usage": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: put_quota_api
        {
            let url = BASE_URL + `/api/v1/quota`;
            // TODO: edit the parameters of the request body.
            let body = {"features": {"erp": "boolean", "fancyReports": "boolean", "taxAutomations": "boolean"}, "maxConnectors": "integer", "maxInvoicesPerMonth": "long", "maxUsers": "integer", "metered": "map", "plan": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "Override saved, effective limits returned": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/reorder-proposals", () => {
        let warehouseId = 'TODO_EDIT_THE_WAREHOUSEID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let configuredOnly = 'TODO_EDIT_THE_CONFIGUREDONLY'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_reorder_proposal
        {
            let url = BASE_URL + `/api/v1/reorder-proposals?configuredOnly=${configuredOnly}&warehouseId=${warehouseId}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/attachments/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_attachment
        {
            let url = BASE_URL + `/api/v1/attachments/${id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: delete_attachment
        {
            let url = BASE_URL + `/api/v1/attachments/${id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/public/jobs", () => {

        // Request No. 1: list_public_postings
        {
            let url = BASE_URL + `/api/v1/public/jobs`;
            let request = http.get(url);

            check(request, {
                "Published postings": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/open-items", () => {
        let customerId = 'TODO_EDIT_THE_CUSTOMER_ID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let reminderLevel1Days = 'TODO_EDIT_THE_REMINDER_LEVEL1_DAYS'; // specify value as there is no example value for this parameter in OpenAPI spec
        let reminderLevel3Days = 'TODO_EDIT_THE_REMINDER_LEVEL3_DAYS'; // specify value as there is no example value for this parameter in OpenAPI spec
        let reminderLevel2Days = 'TODO_EDIT_THE_REMINDER_LEVEL2_DAYS'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_open_items_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/open-items?reminder_level1_days=${reminder_level1_days}&reminder_level2_days=${reminder_level2_days}&reminder_level3_days=${reminder_level3_days}&customer_id=${customer_id}`;
            let request = http.get(url);

            check(request, {
                "List of open invoices": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/gobd", () => {
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec
        let format = 'zip'; // extracted from 'example' field defined at the parameter level of OpenAPI spec

        // Request No. 1: gobd_export_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/gobd?year=${year}&format=${format}`;
            let request = http.get(url);

            check(request, {
                "GoBD/GDPdU export: ZIP (application/zip) with index.xml (IDEA table specification) and one UTF-8 CSV per tax-relevant table; legacy single-journal CSV JSON via ?format=csv": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/search", () => {
        let q = 'TODO_EDIT_THE_Q'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: global_search
        {
            let url = BASE_URL + `/api/v1/search?q=${q}`;
            let request = http.get(url);

            check(request, {
                "": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/kyc-records/", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let includeDeleted = 'TODO_EDIT_THE_INCLUDE_DELETED'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = '1'; // extracted from 'example' field defined at the parameter level of OpenAPI spec

        // Request No. 1: get_kyc_records
        {
            let url = BASE_URL + `/api/v1/kyc-records/?page=${page}&page_size=${page_size}&search=${search}&include_deleted=${include_deleted}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/emissions/factors", () => {

        // Request No. 1: emissions_factors_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/emissions/factors`;
            let request = http.get(url);

            check(request, {
                "Builtin emission factors": (r) => r.status === 200
            });
        }
    });

    group("/user/teams", () => {

        // Request No. 1: list_teams
        {
            let url = BASE_URL + `/user/teams`;
            let request = http.get(url);

            check(request, {
                "List of teams": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: create_team
        {
            let url = BASE_URL + `/user/teams`;
            // TODO: edit the parameters of the request body.
            let body = {"description": "string", "name": "string", "parentTeamId": "uuid"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Team created": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/supplier-conditions/{supplier_condition_id}", () => {
        let supplierConditionId = 'TODO_EDIT_THE_SUPPLIER_CONDITION_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_supplier_condition
        {
            let url = BASE_URL + `/api/v1/supplier-conditions/${supplier_condition_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_supplier_condition
        {
            let url = BASE_URL + `/api/v1/supplier-conditions/${supplier_condition_id}`;
            // TODO: edit the parameters of the request body.
            let body = {"currency": "string", "deliveryTerms": "string", "earlyPaymentDiscountPercent": "string", "isDefault": "boolean", "minimumOrderValue": "string", "notes": "string", "paymentDueDays": "integer", "paymentTerms": "string", "supplierContactId": "string", "supplierName": "string", "volumeDiscountTiers": "oas_any_type_not_mapped"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_supplier_condition
        {
            let url = BASE_URL + `/api/v1/supplier-conditions/${supplier_condition_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/marketplace/connections/{connection_id}/directions", () => {
        let connectionId = 'TODO_EDIT_THE_CONNECTION_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_sync_direction_api
        {
            let url = BASE_URL + `/api/v1/marketplace/connections/${connection_id}/directions`;
            let request = http.get(url);

            check(request, {
                "Current sync directions": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_sync_direction_api
        {
            let url = BASE_URL + `/api/v1/marketplace/connections/${connection_id}/directions`;
            // TODO: edit the parameters of the request body.
            let body = {"directions": "map"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "Sync directions updated": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/liquidity", () => {

        // Request No. 1: get_liquidity
        {
            let url = BASE_URL + `/api/v1/bookkeeping/liquidity`;
            let request = http.get(url);

            check(request, {
                "Liquidity position": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/attachments/{id}/restore", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: attachment_restore
        {
            let url = BASE_URL + `/api/v1/attachments/${id}/restore`;
            let request = http.post(url);

            check(request, {
                "Restored": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/payroll/{id}/elster-export", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: payroll_elster_export
        {
            let url = BASE_URL + `/api/v1/payroll/${id}/elster-export`;
            let request = http.post(url);

            check(request, {
                "CSV export": (r) => r.status === 200
            });
        }
    });

    group("/api/pos/orders/{order_number}/pay", () => {
        let orderNumber = 'TODO_EDIT_THE_ORDER_NUMBER'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: pos_pay_order
        {
            let url = BASE_URL + `/api/pos/orders/${order_number}/pay`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, params);

            check(request, {
                "": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/employees/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_employee
        {
            let url = BASE_URL + `/api/v1/employees/${id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_employee
        {
            let url = BASE_URL + `/api/v1/employees/${id}`;
            // TODO: edit the parameters of the request body.
            let body = {"address": "string", "backupEmployeeId": "uuid", "bic": "string", "city": "string", "country": "countrycode", "dateOfBirth": "date", "departmentId": "uuid", "email": "string", "firstName": "string", "gender": "gender", "hireDate": "date", "hourlyCost": "string", "iban": "string", "jobTitle": "string", "lastLogin": "date", "lastName": "string", "lastUpdated": "date", "monthlySalary": "string", "phone": "string", "state": "string", "status": "employeestatus", "userId": "uuid", "weeklyHours": "string", "zip": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_employee
        {
            let url = BASE_URL + `/api/v1/employees/${id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/warehouses", () => {

        // Request No. 1: create_warehouse
        {
            let url = BASE_URL + `/api/v1/warehouses`;
            // TODO: edit the parameters of the request body.
            let body = {"addressCity": "string", "addressCountry": "countrycode", "addressStreet": "string", "addressZip": "string", "binLocations": "oas_any_type_not_mapped", "code": "string", "isActive": "boolean", "isDefault": "boolean", "name": "string", "notes": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/delivery-appointments/{appointment_id}/status", () => {
        let appointmentId = 'TODO_EDIT_THE_APPOINTMENT_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: update_delivery_appointment_status
        {
            let url = BASE_URL + `/api/v1/delivery-appointments/${appointment_id}/status`;
            // TODO: edit the parameters of the request body.
            let body = {"status": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/absences", () => {

        // Request No. 1: create_absence
        {
            let url = BASE_URL + `/api/v1/absences`;
            // TODO: edit the parameters of the request body.
            let body = {"absenceType": "absencetype", "approvedAt": "date", "approvedBy": "uuid", "employeeId": "uuid", "endDate": "date", "notes": "string", "startDate": "date", "status": "absencestatus"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/users", () => {

        // Request No. 1: list_users
        {
            let url = BASE_URL + `/api/v1/users`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/email-templates/{email_template_id}/render", () => {
        let emailTemplateId = 'TODO_EDIT_THE_EMAIL_TEMPLATE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: render_email_template
        {
            let url = BASE_URL + `/api/v1/email-templates/${email_template_id}/render`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, params);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/silent-partners", () => {

        // Request No. 1: create_silent_partner
        {
            let url = BASE_URL + `/api/v1/silent-partners`;
            // TODO: edit the parameters of the request body.
            let body = {"contractDate": "date", "einlage": "string", "gewinnquotePct": "string", "gewinnvortrag": "string", "instrumentType": "instrumenttype", "kestPflichtig": "boolean", "name": "string", "notes": "string", "verlustVerrechnungskonto": "string", "verlustbeteiligung": "boolean"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/bookkeeping/emissions/report", () => {
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: emissions_report_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/emissions/report?year=${year}`;
            let request = http.get(url);

            check(request, {
                "Emissions report": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/rfqs", () => {

        // Request No. 1: create_rfq
        {
            let url = BASE_URL + `/api/v1/rfqs`;
            // TODO: edit the parameters of the request body.
            let body = {"currency": "string", "lineItems": "oas_any_type_not_mapped", "notes": "string", "requestedDate": "date", "responseDate": "date", "rfqNumber": "string", "status": "rfqstatus", "supplierContactId": "string", "supplierName": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/packing/{order_number}/record-video", () => {
        let orderNumber = 'TODO_EDIT_THE_ORDER_NUMBER'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: record_packing_video
        {
            let url = BASE_URL + `/api/v1/packing/${order_number}/record-video`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Video recorded": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/down-payment-invoices/{id}/pdf", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: download_down_payment_invoice_pdf
        {
            let url = BASE_URL + `/api/v1/down-payment-invoices/${id}/pdf`;
            let request = http.get(url);

            check(request, {
                "PDF placeholder": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/shipments/{shipment_id}/tracking", () => {
        let shipmentId = 'TODO_EDIT_THE_SHIPMENT_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: track_shipment_api
        {
            let url = BASE_URL + `/api/v1/shipments/${shipment_id}/tracking`;
            let request = http.get(url);

            check(request, {
                "Live tracking info": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/ustva/elster-validate", () => {
        let zeitraum = 'TODO_EDIT_THE_ZEITRAUM'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: elster_validate_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/ustva/elster-validate?zeitraum=${zeitraum}`;
            let request = http.post(url);

            check(request, {
                "Validate UStVA XML (mock or ERiC)": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/ebilanz", () => {
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec
        let dateTo = 'TODO_EDIT_THE_DATE_TO'; // specify value as there is no example value for this parameter in OpenAPI spec
        let dateFrom = 'TODO_EDIT_THE_DATE_FROM'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: ebilanz_report_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/ebilanz?year=${year}&date_from=${date_from}&date_to=${date_to}`;
            let request = http.get(url);

            check(request, {
                "E-Bilanz report": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/stille/report", () => {
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: stille_report_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/stille/report?year=${year}`;
            let request = http.get(url);

            check(request, {
                "Gewinn-/Verlustverteilung an stille Gesellschafter (§§ 230–236 HGB)": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/job-applications", () => {
        let postingId = 'TODO_EDIT_THE_POSTINGID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGESIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let status = 'TODO_EDIT_THE_STATUS'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_job_applications
        {
            let url = BASE_URL + `/api/v1/job-applications?postingId=${postingId}&status=${status}&page=${page}&pageSize=${pageSize}`;
            let request = http.get(url);

            check(request, {
                "Applications list": (r) => r.status === 200
            });
        }
    });

    group("/api/pos/registers/{id}/disable", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: pos_disable_register
        {
            let url = BASE_URL + `/api/pos/registers/${id}/disable`;
            let request = http.post(url);

            check(request, {
                "": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/support/ai/suggest", () => {

        // Request No. 1: ai_suggest_api
        {
            let url = BASE_URL + `/api/v1/support/ai/suggest`;
            // TODO: edit the parameters of the request body.
            let body = {"instructions": "string", "messageBody": "string", "ticketId": "uuid"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "AI suggestion": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/usage", () => {
        let meter = 'TODO_EDIT_THE_METER'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_usage_api
        {
            let url = BASE_URL + `/api/v1/usage?meter=${meter}`;
            let request = http.get(url);

            check(request, {
                "Metered usage this period": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/posting-categories/seed/{skr_version}", () => {
        let skrVersion = 'TODO_EDIT_THE_SKR_VERSION'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: seed_posting_categories
        {
            let url = BASE_URL + `/api/v1/posting-categories/seed/${skr_version}`;
            let request = http.post(url);

            check(request, {
                "Seeded": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/supplier-invoices/{supplier_invoice_id}", () => {
        let supplierInvoiceId = 'TODO_EDIT_THE_SUPPLIER_INVOICE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_supplier_invoice
        {
            let url = BASE_URL + `/api/v1/supplier-invoices/${supplier_invoice_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_supplier_invoice
        {
            let url = BASE_URL + `/api/v1/supplier-invoices/${supplier_invoice_id}`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_supplier_invoice
        {
            let url = BASE_URL + `/api/v1/supplier-invoices/${supplier_invoice_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/declarations/{id}/restore", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: declaration_restore
        {
            let url = BASE_URL + `/api/v1/declarations/${id}/restore`;
            let request = http.post(url);

            check(request, {
                "Restored": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/plausibility", () => {
        let dateTo = 'TODO_EDIT_THE_DATE_TO'; // specify value as there is no example value for this parameter in OpenAPI spec
        let dateFrom = 'TODO_EDIT_THE_DATE_FROM'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: plausibility_check_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/plausibility?date_from=${date_from}&date_to=${date_to}`;
            let request = http.get(url);

            check(request, {
                "Plausibility report": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/order-confirmations", () => {

        // Request No. 1: create_confirmation
        {
            let url = BASE_URL + `/api/v1/order-confirmations`;
            // TODO: edit the parameters of the request body.
            let body = {"address": "oas_any_type_not_mapped", "confirmationNumber": "string", "contactId": "string", "contactName": "string", "currency": "string", "files": "oas_any_type_not_mapped", "introduction": "string", "lineItems": "oas_any_type_not_mapped", "precedingSalesVoucherId": "string", "precedingSalesVoucherType": "precedingsalesvouchertype", "remark": "string", "taxCondition": "string", "title": "string", "voucherDate": "date", "voucherStatus": "voucherstatus"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/stock-transfers", () => {

        // Request No. 1: create_stock_transfer
        {
            let url = BASE_URL + `/api/v1/stock-transfers`;
            // TODO: edit the parameters of the request body.
            let body = {"lineItems": "oas_any_type_not_mapped", "notes": "string", "sourceWarehouseId": "string", "status": "stocktransferstatus", "targetWarehouseId": "string", "transferDate": "date", "transferNumber": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/products/{product_id}/restore", () => {
        let productId = 'TODO_EDIT_THE_PRODUCT_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: product_restore
        {
            let url = BASE_URL + `/api/v1/products/${product_id}/restore`;
            let request = http.post(url);

            check(request, {
                "Restored": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/absences/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_absence
        {
            let url = BASE_URL + `/api/v1/absences/${id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_absence
        {
            let url = BASE_URL + `/api/v1/absences/${id}`;
            // TODO: edit the parameters of the request body.
            let body = {"absenceType": "absencetype", "approvedAt": "date", "approvedBy": "uuid", "employeeId": "uuid", "endDate": "date", "notes": "string", "startDate": "date", "status": "absencestatus"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_absence
        {
            let url = BASE_URL + `/api/v1/absences/${id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/goods-receipts", () => {

        // Request No. 1: create_goods_receipt
        {
            let url = BASE_URL + `/api/v1/goods-receipts`;
            // TODO: edit the parameters of the request body.
            let body = {"grNumber": "string", "lineItems": "oas_any_type_not_mapped", "notes": "string", "purchaseOrderId": "string", "receiptDate": "date", "supplierContactId": "string", "supplierName": "string", "warehouseId": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/rfqs/{rfq_id}", () => {
        let rfqId = 'TODO_EDIT_THE_RFQ_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_rfq
        {
            let url = BASE_URL + `/api/v1/rfqs/${rfq_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_rfq
        {
            let url = BASE_URL + `/api/v1/rfqs/${rfq_id}`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_rfq
        {
            let url = BASE_URL + `/api/v1/rfqs/${rfq_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/public/jobs/inbound-email", () => {

        // Request No. 1: inbound_email
        {
            let url = BASE_URL + `/api/v1/public/jobs/inbound-email`;
            let request = http.post(url);

            check(request, {
                "Application received": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/labor-costs", () => {
        let from = 'TODO_EDIT_THE_FROM'; // specify value as there is no example value for this parameter in OpenAPI spec
        let to = 'TODO_EDIT_THE_TO'; // specify value as there is no example value for this parameter in OpenAPI spec
        let groupBy = 'TODO_EDIT_THE_GROUP_BY'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_labor_costs
        {
            let url = BASE_URL + `/api/v1/labor-costs?from=${from}&to=${to}&group_by=${group_by}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/admin/storage/mirror", () => {

        // Request No. 1: trigger_mirror
        {
            let url = BASE_URL + `/api/v1/admin/storage/mirror`;
            let request = http.post(url);

            check(request, {
                "Mirror job queued": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/proforma-invoices/{proforma_id}", () => {
        let proformaId = 'TODO_EDIT_THE_PROFORMA_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_proforma_invoice
        {
            let url = BASE_URL + `/api/v1/proforma-invoices/${proforma_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_proforma_invoice
        {
            let url = BASE_URL + `/api/v1/proforma-invoices/${proforma_id}`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_proforma_invoice
        {
            let url = BASE_URL + `/api/v1/proforma-invoices/${proforma_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/bookkeeping/paygap/auskunft/{employee_id}", () => {
        let employeeId = 'TODO_EDIT_THE_EMPLOYEE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: paygap_auskunft_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/paygap/auskunft/${employee_id}`;
            let request = http.get(url);

            check(request, {
                "Individual pay information (§ 10-14 EntgTranspG)": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/recurring-templates/", () => {

        // Request No. 1: list_recurring_templates
        {
            let url = BASE_URL + `/api/v1/recurring-templates/`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/delivery-dates/{delivery_date_id}/status", () => {
        let deliveryDateId = 'TODO_EDIT_THE_DELIVERY_DATE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: update_delivery_date_status
        {
            let url = BASE_URL + `/api/v1/delivery-dates/${delivery_date_id}/status`;
            // TODO: edit the parameters of the request body.
            let body = {"status": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/returns", () => {

        // Request No. 1: create_return_order
        {
            let url = BASE_URL + `/api/v1/returns`;
            // TODO: edit the parameters of the request body.
            let body = {"customerContactId": "string", "customerName": "string", "lineItems": "oas_any_type_not_mapped", "notes": "string", "orderId": "string", "orderNumber": "string", "returnNumber": "string", "returnReason": "string", "status": "returnorderstatus", "warehouseId": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/tax-rates/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: update_tax_rate
        {
            let url = BASE_URL + `/api/v1/tax-rates/${id}`;
            // TODO: edit the parameters of the request body.
            let body = {"countryCode": "string", "effectiveFrom": "date", "isDefault": "boolean", "name": "string", "ratePercent": "long"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "Tax rate updated": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: delete_tax_rate
        {
            let url = BASE_URL + `/api/v1/tax-rates/${id}`;
            let request = http.del(url);

            check(request, {
                "Tax rate deleted": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/support/leads/{lead_id}", () => {
        let leadId = 'TODO_EDIT_THE_LEAD_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: update_lead_api
        {
            let url = BASE_URL + `/api/v1/support/leads/${lead_id}`;
            // TODO: edit the parameters of the request body.
            let body = {"company": "string", "convertedAt": "date", "createdAt": "date", "email": "string", "firstContactAt": "date", "name": "string", "notes": "string", "phone": "string", "score": "integer", "source": "string", "status": "leadstatus", "tags": "oas_any_type_not_mapped", "tenantId": "uuid", "updatedAt": "date"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "Lead updated": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/event-subscriptions/{subscription_id}", () => {
        let subscriptionId = 'TODO_EDIT_THE_SUBSCRIPTION_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: delete_event_subscription
        {
            let url = BASE_URL + `/api/v1/event-subscriptions/${subscription_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/orders/by-ext-ref/{ext_ref}", () => {
        let extRef = 'TODO_EDIT_THE_EXT_REF'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: find_order_by_external_ref
        {
            let url = BASE_URL + `/api/v1/orders/by-ext-ref/${ext_ref}`;
            let request = http.get(url);

            check(request, {
                "Order found": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/packing/{order_number}/complete", () => {
        let orderNumber = 'TODO_EDIT_THE_ORDER_NUMBER'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: complete_packing
        {
            let url = BASE_URL + `/api/v1/packing/${order_number}/complete`;
            // TODO: edit the parameters of the request body.
            let body = {"notes": "string", "orderNumber": "string", "shipmentId": "string", "videoUrl": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Packing completed": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/delivery-notes/", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let includeDeleted = 'TODO_EDIT_THE_INCLUDE_DELETED'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = '1'; // extracted from 'example' field defined at the parameter level of OpenAPI spec

        // Request No. 1: list_delivery_notes
        {
            let url = BASE_URL + `/api/v1/delivery-notes/?page=${page}&page_size=${page_size}&search=${search}&include_deleted=${include_deleted}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/shipping/rates", () => {

        // Request No. 1: get_rates_api
        {
            let url = BASE_URL + `/api/v1/shipping/rates`;
            // TODO: edit the parameters of the request body.
            let body = {"customer": {"annualVolume": "integer", "isRegistered": "boolean"}, "packages": "list", "recipient": {"city": "string", "company": "string", "country": "string", "email": "string", "name": "string", "phone": "string", "street": "string", "streetNumber": "string", "zip": "string"}, "sender": {"city": "string", "company": "string", "country": "string", "email": "string", "name": "string", "phone": "string", "street": "string", "streetNumber": "string", "zip": "string"}};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Shipping rates from configured providers": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/delivery-dates", () => {

        // Request No. 1: create_delivery_date
        {
            let url = BASE_URL + `/api/v1/delivery-dates`;
            // TODO: edit the parameters of the request body.
            let body = {"customerId": "string", "fulfilledDate": "date", "note": "string", "orderNumber": "string", "originalDate": "date", "productId": "string", "promisedDate": "date", "status": "deliverydatestatus"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/shareholders", () => {

        // Request No. 1: create_shareholder
        {
            let url = BASE_URL + `/api/v1/shareholders`;
            // TODO: edit the parameters of the request body.
            let body = {"address": "string", "birthDate": "date", "email": "string", "firstName": "string", "lastName": "string", "shareNumber": "string", "shares": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/purchase-orders/", () => {
        let supplierName = 'TODO_EDIT_THE_SUPPLIER_NAME'; // specify value as there is no example value for this parameter in OpenAPI spec
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let status = 'TODO_EDIT_THE_STATUS'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_purchase_orders
        {
            let url = BASE_URL + `/api/v1/purchase-orders/?page=${page}&page_size=${page_size}&status=${status}&supplier_name=${supplier_name}&search=${search}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/shipping-thresholds", () => {

        // Request No. 1: create_shipping_threshold
        {
            let url = BASE_URL + `/api/v1/shipping-thresholds`;
            // TODO: edit the parameters of the request body.
            let body = {"isActive": "boolean", "maxSellable": "long", "name": "string", "notes": "string", "productId": "uuid", "reserveStock": "long", "warehouseId": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/replenishments", () => {
        let sourceWarehouseId = 'TODO_EDIT_THE_SOURCEWAREHOUSEID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let targetWarehouseId = 'TODO_EDIT_THE_TARGETWAREHOUSEID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_replenishments
        {
            let url = BASE_URL + `/api/v1/replenishments?targetWarehouseId=${targetWarehouseId}&sourceWarehouseId=${sourceWarehouseId}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/order-confirmations/", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let includeDeleted = 'TODO_EDIT_THE_INCLUDE_DELETED'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = '1'; // extracted from 'example' field defined at the parameter level of OpenAPI spec

        // Request No. 1: list_confirmations
        {
            let url = BASE_URL + `/api/v1/order-confirmations/?page=${page}&page_size=${page_size}&search=${search}&include_deleted=${include_deleted}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/delivery-dates/performance", () => {
        let orderNumber = 'TODO_EDIT_THE_ORDERNUMBER'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGESIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let from = 'TODO_EDIT_THE_FROM'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let to = 'TODO_EDIT_THE_TO'; // specify value as there is no example value for this parameter in OpenAPI spec
        let status = 'TODO_EDIT_THE_STATUS'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_delivery_performance
        {
            let url = BASE_URL + `/api/v1/delivery-dates/performance?page=${page}&pageSize=${pageSize}&orderNumber=${orderNumber}&status=${status}&from=${from}&to=${to}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/datev/import", () => {

        // Request No. 1: datev_import_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/datev/import`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Parsed DATEV CSV rows": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/supplier-invoices", () => {

        // Request No. 1: create_supplier_invoice
        {
            let url = BASE_URL + `/api/v1/supplier-invoices`;
            // TODO: edit the parameters of the request body.
            let body = {"currency": "string", "goodsReceiptId": "string", "invoiceDate": "date", "invoiceNumber": "string", "lineItems": "oas_any_type_not_mapped", "notes": "string", "purchaseOrderId": "string", "status": "supplierinvoicestatus", "supplierContactId": "string", "supplierName": "string", "totalGrossAmount": "string", "totalNetAmount": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/bookkeeping/ustva", () => {
        let zeitraum = 'TODO_EDIT_THE_ZEITRAUM'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: ustva_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/ustva?zeitraum=${zeitraum}`;
            let request = http.get(url);

            check(request, {
                "UStVA Ergebnis": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/payroll/{id}/pay", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: payroll_pay
        {
            let url = BASE_URL + `/api/v1/payroll/${id}/pay`;
            // TODO: edit the parameters of the request body.
            let body = {"paymentDate": "date"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/payment-gateways/{gateway_id}", () => {
        let gatewayId = 'TODO_EDIT_THE_GATEWAY_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: update_payment_gateway_api
        {
            let url = BASE_URL + `/api/v1/payment-gateways/${gateway_id}`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: delete_payment_gateway_api
        {
            let url = BASE_URL + `/api/v1/payment-gateways/${gateway_id}`;
            let request = http.del(url);

            check(request, {
                "Deleted": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/product-variants/", () => {
        let productId = 'TODO_EDIT_THE_PRODUCT_ID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let isActive = 'TODO_EDIT_THE_IS_ACTIVE'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_product_variants
        {
            let url = BASE_URL + `/api/v1/product-variants/?page=${page}&page_size=${page_size}&product_id=${product_id}&is_active=${is_active}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/subscription", () => {

        // Request No. 1: get_subscription_api
        {
            let url = BASE_URL + `/api/v1/subscription`;
            let request = http.get(url);

            check(request, {
                "Tenant subscription overview": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/marketplace/connections", () => {

        // Request No. 1: list_connections_api
        {
            let url = BASE_URL + `/api/v1/marketplace/connections`;
            let request = http.get(url);

            check(request, {
                "List of connections": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: create_connection_api
        {
            let url = BASE_URL + `/api/v1/marketplace/connections`;
            // TODO: edit the parameters of the request body.
            let body = {"apiKey": "string", "apiSecret": "string", "config": "oas_any_type_not_mapped", "label": "string", "platform": "string", "shopDomain": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/pos/kasse/export", () => {

        // Request No. 1: pos_kasse_export
        {
            let url = BASE_URL + `/api/pos/kasse/export`;
            let request = http.get(url);

            check(request, {
                "": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/quotations/{quotation_id}/pdf", () => {
        let quotationId = 'TODO_EDIT_THE_QUOTATION_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: download_quotation_pdf
        {
            let url = BASE_URL + `/api/v1/quotations/${quotation_id}/pdf`;
            let request = http.get(url);

            check(request, {
                "PDF file": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/automations/{key}/trigger", () => {
        let key = 'TODO_EDIT_THE_KEY'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: trigger_automation
        {
            let url = BASE_URL + `/api/v1/automations/${key}/trigger`;
            let request = http.post(url);

            check(request, {
                "": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/kst", () => {
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec
        let gewinn = 'TODO_EDIT_THE_GEWINN'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: kst_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/kst?year=${year}&gewinn=${gewinn}`;
            let request = http.get(url);

            check(request, {
                "Körperschaftsteuer Ergebnis": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/delivery-appointments", () => {
        let warehouseId = 'TODO_EDIT_THE_WAREHOUSE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let from = 'TODO_EDIT_THE_FROM'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let to = 'TODO_EDIT_THE_TO'; // specify value as there is no example value for this parameter in OpenAPI spec
        let status = 'TODO_EDIT_THE_STATUS'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_delivery_appointments
        {
            let url = BASE_URL + `/api/v1/delivery-appointments?page=${page}&page_size=${page_size}&status=${status}&warehouse_id=${warehouse_id}&from=${from}&to=${to}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: create_delivery_appointment
        {
            let url = BASE_URL + `/api/v1/delivery-appointments`;
            // TODO: edit the parameters of the request body.
            let body = {"email": "string", "notes": "string", "phone": "string", "requestedDate": "date", "status": "deliveryappointmentstatus", "supplierName": "string", "timeSlot": "string", "warehouseId": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/support/ai/workers", () => {

        // Request No. 1: list_workers_api
        {
            let url = BASE_URL + `/api/v1/support/ai/workers`;
            let request = http.get(url);

            check(request, {
                "List AI workers": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: create_worker_api
        {
            let url = BASE_URL + `/api/v1/support/ai/workers`;
            // TODO: edit the parameters of the request body.
            let body = {"autoReply": "boolean", "maxToolCalls": "integer", "model": "string", "name": "string", "provider": "string", "systemPrompt": "string", "triggerOn": "list"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Worker created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/proforma-invoices/{proforma_id}/convert", () => {
        let proformaId = 'TODO_EDIT_THE_PROFORMA_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: convert_proforma_to_invoice
        {
            let url = BASE_URL + `/api/v1/proforma-invoices/${proforma_id}/convert`;
            let request = http.post(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/employees/{id}/payroll-summary", () => {
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_employee_payroll_summary
        {
            let url = BASE_URL + `/api/v1/employees/${id}/payroll-summary?year=${year}`;
            let request = http.get(url);

            check(request, {
                "Payroll summary for the employee": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/ustva/elster-xml", () => {
        let zeitraum = 'TODO_EDIT_THE_ZEITRAUM'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: elster_xml_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/ustva/elster-xml?zeitraum=${zeitraum}`;
            let request = http.get(url);

            check(request, {
                "ELSTER UStVA XML template (manual upload)": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/invoices", () => {

        // Request No. 1: create_invoice
        {
            let url = BASE_URL + `/api/v1/invoices`;
            // TODO: edit the parameters of the request body.
            let body = {"attachments": "oas_any_type_not_mapped", "billingPeriodEnd": "date", "billingPeriodStart": "date", "cancellationDate": "date", "cancellationInvoiceId": "string", "cancellationReason": "string", "contractId": "uuid", "currency": "currencycode", "customerId": "string", "discountAmount": "string", "discountDays": "integer", "discountPercentage": "string", "documentType": "documenttype", "dunningLevel": "integer", "inputVatAmount": "string", "inputVatDeductible": "boolean", "inputVatPercentage": "string", "introductionText": "string", "invoiceType": "invoicetype", "isCancelled": "boolean", "isDraft": "boolean", "isEuAcquisition": "boolean", "isEuDelivery": "boolean", "isIntraCommunityAcquisition": "boolean", "isReverseCharge": "boolean", "issueDate": "date", "ledgerAccount": "string", "lineItems": "oas_any_type_not_mapped", "margin25a": "boolean", "margin25aGross": "string", "margin25aPurchasePrice": "string", "notes": "string", "orderNumber": "string", "originalPdfPath": "string", "paidAmount": "string", "paymentDueDate": "date", "paymentStatus": "paymentstatus", "paymentTermsText": "string", "precedingSalesVoucherId": "string", "precedingSalesVoucherType": "precedingsalesvouchertype", "receiptConfirmationAvailable": "boolean", "relatedInvoiceId": "uuid", "relationshipType": "string", "senderSnapshot": "oas_any_type_not_mapped", "sentAt": "date", "servicePeriodEnd": "date", "servicePeriodStart": "date", "status": "invoicestatus", "subtotal": "string", "supplierId": "string", "taxExemptionReason": "string", "totalAmount": "string", "totalTax": "string", "vatCountry": "countrycode", "vatSpecialCase": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/notifications/unread-count", () => {

        // Request No. 1: unread_count
        {
            let url = BASE_URL + `/api/v1/notifications/unread-count`;
            let request = http.get(url);

            check(request, {
                "Number of unread notifications": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/delivery-notes/{delivery_note_id}/restore", () => {
        let deliveryNoteId = 'TODO_EDIT_THE_DELIVERY_NOTE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: deliverynote_restore
        {
            let url = BASE_URL + `/api/v1/delivery-notes/${delivery_note_id}/restore`;
            let request = http.post(url);

            check(request, {
                "Restored": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/training-assignments", () => {

        // Request No. 1: create_training_assignment
        {
            let url = BASE_URL + `/api/v1/training-assignments`;
            // TODO: edit the parameters of the request body.
            let body = {"assignedBy": "uuid", "dueDate": "date", "employeeId": "uuid", "notes": "string", "status": "assignmentstatus", "trainingId": "uuid"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/user/profile", () => {

        // Request No. 1: user_profile
        {
            let url = BASE_URL + `/user/profile`;
            let request = http.get(url);

            check(request, {
                "User profile": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_profile
        {
            let url = BASE_URL + `/user/profile`;
            // TODO: edit the parameters of the request body.
            let body = {"avatarUrl": "string", "firstName": "string", "lastName": "string", "name": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "Profile updated": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/invoices/{id}/pdf-url", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_invoice_pdf_url
        {
            let url = BASE_URL + `/api/v1/invoices/${id}/pdf-url`;
            let request = http.get(url);

            check(request, {
                "Presigned download URL": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/group-figures/", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let includeDeleted = 'TODO_EDIT_THE_INCLUDE_DELETED'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = '1'; // extracted from 'example' field defined at the parameter level of OpenAPI spec

        // Request No. 1: get_group_figures
        {
            let url = BASE_URL + `/api/v1/group-figures/?page=${page}&page_size=${page_size}&search=${search}&include_deleted=${include_deleted}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/goods-receipts/{goods_receipt_id}", () => {
        let goodsReceiptId = 'TODO_EDIT_THE_GOODS_RECEIPT_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_goods_receipt
        {
            let url = BASE_URL + `/api/v1/goods-receipts/${goods_receipt_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: delete_goods_receipt
        {
            let url = BASE_URL + `/api/v1/goods-receipts/${goods_receipt_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/auth/totp/enable", () => {

        // Request No. 1: totp_enable
        {
            let url = BASE_URL + `/auth/totp/enable`;
            // TODO: edit the parameters of the request body.
            let body = {"code": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "TOTP enabled successfully": (r) => r.status === 200
            });
        }
    });

    group("/user/remove", () => {

        // Request No. 1: remove_user_from_org
        {
            let url = BASE_URL + `/user/remove`;
            // TODO: edit the parameters of the request body.
            let body = {"email": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            // this is a DELETE method request - if params are also set, empty body must be passed
            let request = http.del(url, {} , params);

            check(request, {
                "User removed": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/shipping-rules/", () => {
        let country = 'TODO_EDIT_THE_COUNTRY'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_shipping_rules
        {
            let url = BASE_URL + `/api/v1/shipping-rules/?page=${page}&page_size=${page_size}&country=${country}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/support/channels", () => {

        // Request No. 1: list_channels_api
        {
            let url = BASE_URL + `/api/v1/support/channels`;
            let request = http.get(url);

            check(request, {
                "List of channels": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: create_channel_api
        {
            let url = BASE_URL + `/api/v1/support/channels`;
            // TODO: edit the parameters of the request body.
            let body = {"channelType": "string", "config": "oas_any_type_not_mapped", "name": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Channel created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/credit-notes", () => {

        // Request No. 1: create_credit_note
        {
            let url = BASE_URL + `/api/v1/credit-notes`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/pos/registers", () => {

        // Request No. 1: pos_list_registers
        {
            let url = BASE_URL + `/api/pos/registers`;
            let request = http.get(url);

            check(request, {
                "": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: pos_create_register
        {
            let url = BASE_URL + `/api/pos/registers`;
            // TODO: edit the parameters of the request body.
            let body = {"name": "string", "status": "posregisterstatus"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/invoices/{id}/restore", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: invoice_restore
        {
            let url = BASE_URL + `/api/v1/invoices/${id}/restore`;
            let request = http.post(url);

            check(request, {
                "Restored": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/product-attributes", () => {

        // Request No. 1: create_product_attribute
        {
            let url = BASE_URL + `/api/v1/product-attributes`;
            // TODO: edit the parameters of the request body.
            let body = {"isFilterable": "boolean", "name": "string", "position": "integer", "productId": "uuid", "unit": "string", "value": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/down-payment-invoices/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_down_payment_invoice
        {
            let url = BASE_URL + `/api/v1/down-payment-invoices/${id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/auth/totp/setup", () => {

        // Request No. 1: totp_setup
        {
            let url = BASE_URL + `/auth/totp/setup`;
            let request = http.get(url);

            check(request, {
                "TOTP setup data": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/order-confirmations/{confirmation_id}", () => {
        let confirmationId = 'TODO_EDIT_THE_CONFIRMATION_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_confirmation
        {
            let url = BASE_URL + `/api/v1/order-confirmations/${confirmation_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: delete_confirmation
        {
            let url = BASE_URL + `/api/v1/order-confirmations/${confirmation_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/activities/", () => {
        let contactId = 'TODO_EDIT_THE_CONTACTID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let overdueOnly = 'TODO_EDIT_THE_OVERDUEONLY'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGESIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let activityType = 'TODO_EDIT_THE_ACTIVITYTYPE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let assignedTo = 'TODO_EDIT_THE_ASSIGNEDTO'; // specify value as there is no example value for this parameter in OpenAPI spec
        let status = 'TODO_EDIT_THE_STATUS'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_activities
        {
            let url = BASE_URL + `/api/v1/activities/?page=${page}&pageSize=${pageSize}&contactId=${contactId}&activityType=${activityType}&status=${status}&assignedTo=${assignedTo}&overdueOnly=${overdueOnly}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/communications", () => {

        // Request No. 1: create_communication
        {
            let url = BASE_URL + `/api/v1/communications`;
            // TODO: edit the parameters of the request body.
            let body = {"body": "string", "channel": "communicationchannel", "contactId": "string", "counterparty": "string", "direction": "communicationdirection", "occurredAt": "date", "subject": "string", "tags": "oas_any_type_not_mapped"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/payments/", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let includeDeleted = 'TODO_EDIT_THE_INCLUDE_DELETED'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = '1'; // extracted from 'example' field defined at the parameter level of OpenAPI spec

        // Request No. 1: get_payments
        {
            let url = BASE_URL + `/api/v1/payments/?page=${page}&page_size=${page_size}&search=${search}&include_deleted=${include_deleted}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/webhook-subscriptions/{subscription_id}", () => {
        let subscriptionId = 'TODO_EDIT_THE_SUBSCRIPTION_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: update_subscription
        {
            let url = BASE_URL + `/api/v1/webhook-subscriptions/${subscription_id}`;
            // TODO: edit the parameters of the request body.
            let body = {"eventType": "string", "isActive": "boolean", "name": "string", "secret": "string", "url": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: delete_subscription
        {
            let url = BASE_URL + `/api/v1/webhook-subscriptions/${subscription_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/user/api-key", () => {

        // Request No. 1: generate_api_key
        {
            let url = BASE_URL + `/user/api-key`;
            let request = http.post(url);

            check(request, {
                "API key generated": (r) => r.status === 200
            });
        }
    });

    group("/api/pos/kasse/entries", () => {

        // Request No. 1: pos_kasse_entries
        {
            let url = BASE_URL + `/api/pos/kasse/entries`;
            let request = http.get(url);

            check(request, {
                "": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/payroll/{id}/autopay", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: payroll_autopay
        {
            let url = BASE_URL + `/api/v1/payroll/${id}/autopay`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, params);

            check(request, {
                "": (r) => r.status === 200
            });
        }
    });

    group("/auth/logout", () => {

        // Request No. 1: logout
        {
            let url = BASE_URL + `/auth/logout`;
            let request = http.post(url);

            check(request, {
                "Logged out successfully": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/compliance-trainings", () => {

        // Request No. 1: create_compliance_training
        {
            let url = BASE_URL + `/api/v1/compliance-trainings`;
            // TODO: edit the parameters of the request body.
            let body = {"assignable": "boolean", "code": "string", "description": "string", "passScore": "integer", "pluginPlatform": "string", "source": "trainingsource", "title": "string", "validityMonths": "integer"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/kyc-records/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_kyc_record
        {
            let url = BASE_URL + `/api/v1/kyc-records/${id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_kyc_record
        {
            let url = BASE_URL + `/api/v1/kyc-records/${id}`;
            // TODO: edit the parameters of the request body.
            let body = {"customerId": "string", "customerName": "string", "kycDate": "date", "notes": "string", "retentionUntil": "date", "riskAssessment": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_kyc_record
        {
            let url = BASE_URL + `/api/v1/kyc-records/${id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/communications/{communication_id}", () => {
        let communicationId = 'TODO_EDIT_THE_COMMUNICATION_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_communication
        {
            let url = BASE_URL + `/api/v1/communications/${communication_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_communication
        {
            let url = BASE_URL + `/api/v1/communications/${communication_id}`;
            // TODO: edit the parameters of the request body.
            let body = {"body": "string", "channel": "communicationchannel", "contactId": "string", "counterparty": "string", "direction": "communicationdirection", "occurredAt": "date", "subject": "string", "tags": "oas_any_type_not_mapped"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_communication
        {
            let url = BASE_URL + `/api/v1/communications/${communication_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/settings/smtp", () => {

        // Request No. 1: get_smtp_config_api
        {
            let url = BASE_URL + `/api/v1/settings/smtp`;
            let request = http.get(url);

            check(request, {
                "Tenant SMTP config (null if unset)": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: save_smtp_config_api
        {
            let url = BASE_URL + `/api/v1/settings/smtp`;
            // TODO: edit the parameters of the request body.
            let body = {"encryption": "smtpencryption", "fromAddress": "string", "fromName": "string", "host": "string", "password": "string", "port": "integer", "timeoutSeconds": "long", "username": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "Tenant SMTP config saved": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/payment-conditions", () => {

        // Request No. 1: list_payment_conditions_api
        {
            let url = BASE_URL + `/api/v1/payment-conditions`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/payments/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_payment
        {
            let url = BASE_URL + `/api/v1/payments/${id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_payment
        {
            let url = BASE_URL + `/api/v1/payments/${id}`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_payment
        {
            let url = BASE_URL + `/api/v1/payments/${id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/products", () => {

        // Request No. 1: create_product_api
        {
            let url = BASE_URL + `/api/v1/products`;
            // TODO: edit the parameters of the request body.
            let body = {"availability": "string", "barcode": "string", "brand": "string", "categoryId": "string", "condition": "string", "defaultLedgerAccount": "string", "defaultPrice": "string", "defaultPriceFormulaId": "uuid", "defaultTaxRate": "string", "description": "string", "gtin": "string", "height": "string", "imageLink": "string", "images": "oas_any_type_not_mapped", "isTaxable": "boolean", "length": "string", "link": "string", "maxStock": "long", "minStock": "long", "mpn": "string", "name": "string", "packageHeight": "string", "packageLength": "string", "packageWeightUnit": "string", "packageWeightValue": "string", "packageWidth": "string", "productCode": "string", "productType": "string", "purchasePrice": "string", "reorderQuantity": "long", "salePrice": "string", "shippingPrice": "string", "shippingRequiresInsurance": "boolean", "sku": "string", "stockQuantity": "long", "tags": "oas_any_type_not_mapped", "taxPrice": "string", "trackBatch": "boolean", "trackSerial": "boolean", "unit": "oas_any_type_not_mapped", "weightUnit": "string", "weightValue": "string", "width": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/tax-rates", () => {

        // Request No. 1: list_tax_rates
        {
            let url = BASE_URL + `/api/v1/tax-rates`;
            let request = http.get(url);

            check(request, {
                "Tenant's tax rates": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: create_tax_rate
        {
            let url = BASE_URL + `/api/v1/tax-rates`;
            // TODO: edit the parameters of the request body.
            let body = {"countryCode": "string", "effectiveFrom": "date", "isDefault": "boolean", "name": "string", "ratePercent": "long"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Tax rate created": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/payments", () => {

        // Request No. 1: create_payment
        {
            let url = BASE_URL + `/api/v1/payments`;
            // TODO: edit the parameters of the request body.
            let body = {"amount": "string", "attachment": "oas_any_type_not_mapped", "currency": "string", "customerId": "string", "description": "string", "metadata": "oas_any_type_not_mapped", "method": "paymentmethod", "paymentDate": "date", "reference": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/rfqs/{rfq_id}/status", () => {
        let rfqId = 'TODO_EDIT_THE_RFQ_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: update_rfq_status
        {
            let url = BASE_URL + `/api/v1/rfqs/${rfq_id}/status`;
            // TODO: edit the parameters of the request body.
            let body = {"status": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/returns/{return_order_id}/status", () => {
        let returnOrderId = 'TODO_EDIT_THE_RETURN_ORDER_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: update_return_order_status
        {
            let url = BASE_URL + `/api/v1/returns/${return_order_id}/status`;
            // TODO: edit the parameters of the request body.
            let body = {"status": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/users/{user_id}/permissions", () => {
        let userId = 'TODO_EDIT_THE_USER_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: update_user_permissions
        {
            let url = BASE_URL + `/api/v1/users/${user_id}/permissions`;
            // TODO: edit the parameters of the request body.
            let body = {"permissions": "list"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/delivery-appointments/{appointment_id}", () => {
        let appointmentId = 'TODO_EDIT_THE_APPOINTMENT_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_delivery_appointment
        {
            let url = BASE_URL + `/api/v1/delivery-appointments/${appointment_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_delivery_appointment
        {
            let url = BASE_URL + `/api/v1/delivery-appointments/${appointment_id}`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_delivery_appointment
        {
            let url = BASE_URL + `/api/v1/delivery-appointments/${appointment_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/quotations/{quotation_id}/restore", () => {
        let quotationId = 'TODO_EDIT_THE_QUOTATION_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: quotation_restore
        {
            let url = BASE_URL + `/api/v1/quotations/${quotation_id}/restore`;
            let request = http.post(url);

            check(request, {
                "Restored": (r) => r.status === 200
            });
        }
    });

    group("/user/change-password", () => {

        // Request No. 1: change_password
        {
            let url = BASE_URL + `/user/change-password`;
            // TODO: edit the parameters of the request body.
            let body = {"currentPassword": "string", "newPassword": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Password changed": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/shareholders/", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let includeDeleted = 'TODO_EDIT_THE_INCLUDE_DELETED'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = '1'; // extracted from 'example' field defined at the parameter level of OpenAPI spec

        // Request No. 1: get_shareholders
        {
            let url = BASE_URL + `/api/v1/shareholders/?page=${page}&page_size=${page_size}&search=${search}&include_deleted=${include_deleted}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/pos/orders", () => {
        let status = 'TODO_EDIT_THE_STATUS'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: pos_list_orders
        {
            let url = BASE_URL + `/api/pos/orders?status=${status}`;
            let request = http.get(url);

            check(request, {
                "": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: pos_create_order
        {
            let url = BASE_URL + `/api/pos/orders`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, params);

            check(request, {
                "": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/invoices/", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let includeDeleted = 'TODO_EDIT_THE_INCLUDE_DELETED'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = '1'; // extracted from 'example' field defined at the parameter level of OpenAPI spec

        // Request No. 1: get_invoices
        {
            let url = BASE_URL + `/api/v1/invoices/?page=${page}&page_size=${page_size}&search=${search}&include_deleted=${include_deleted}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/order/{order_number}", () => {
        let orderNumber = 'TODO_EDIT_THE_ORDER_NUMBER'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_order
        {
            let url = BASE_URL + `/api/v1/order/${order_number}`;
            let request = http.get(url);

            check(request, {
                "Order found": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/payroll/{id}/pdf", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: payroll_pdf
        {
            let url = BASE_URL + `/api/v1/payroll/${id}/pdf`;
            let request = http.get(url);

            check(request, {
                "PDF summary": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/price-tiers/", () => {
        let customerGroupId = 'TODO_EDIT_THE_CUSTOMER_GROUP_ID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let productId = 'TODO_EDIT_THE_PRODUCT_ID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_price_tiers
        {
            let url = BASE_URL + `/api/v1/price-tiers/?page=${page}&page_size=${page_size}&product_id=${product_id}&customer_group_id=${customer_group_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/trainings/submit-result", () => {

        // Request No. 1: submit_training_result
        {
            let url = BASE_URL + `/api/v1/trainings/submit-result`;
            // TODO: edit the parameters of the request body.
            let body = {"answers": "list", "assignmentId": "uuid", "score": "integer", "trainingCode": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Result recorded": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/reorder-proposals/apply", () => {
        let warehouseId = 'TODO_EDIT_THE_WAREHOUSEID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let configuredOnly = 'TODO_EDIT_THE_CONFIGUREDONLY'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: apply_reorder_proposal
        {
            let url = BASE_URL + `/api/v1/reorder-proposals/apply?configuredOnly=${configuredOnly}&warehouseId=${warehouseId}`;
            let request = http.post(url);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/bookkeeping/datev/preview", () => {
        let dateTo = 'TODO_EDIT_THE_DATE_TO'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let accountSchema = 'TODO_EDIT_THE_ACCOUNT_SCHEMA'; // specify value as there is no example value for this parameter in OpenAPI spec
        let dateFrom = 'TODO_EDIT_THE_DATE_FROM'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: datev_preview_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/datev/preview?account_schema=${account_schema}&date_from=${date_from}&date_to=${date_to}&page=${page}&page_size=${page_size}`;
            let request = http.get(url);

            check(request, {
                "DATEV booking preview": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/eks", () => {
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: eks_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/eks?year=${year}`;
            let request = http.get(url);

            check(request, {
                "Anlage EKS Ergebnis": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/attachments/", () => {
        let contactId = 'TODO_EDIT_THE_CONTACTID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGESIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_attachments
        {
            let url = BASE_URL + `/api/v1/attachments/?page=${page}&pageSize=${pageSize}&contactId=${contactId}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/customers/{customer_id}", () => {
        let customerId = 'TODO_EDIT_THE_CUSTOMER_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_customer
        {
            let url = BASE_URL + `/api/v1/customers/${customer_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_customer
        {
            let url = BASE_URL + `/api/v1/customers/${customer_id}`;
            // TODO: edit the parameters of the request body.
            let body = {"address": "oas_any_type_not_mapped", "contactPerson": "string", "email": "string", "externalOrderNumber": "string", "name": "string", "paymentGracePeriodDays": "integer", "phone": "string", "vatId": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_customer
        {
            let url = BASE_URL + `/api/v1/customers/${customer_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/marketplace/oauth/callback", () => {

        // Request No. 1: oauth_callback_api
        {
            let url = BASE_URL + `/api/v1/marketplace/oauth/callback`;
            // TODO: edit the parameters of the request body.
            let body = {"code": "string", "config": "oas_any_type_not_mapped", "connectionId": "string", "platform": "string", "shopDomain": "string", "state": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Connection created/updated": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/job-applications/{application_id}/score", () => {
        let applicationId = 'TODO_EDIT_THE_APPLICATION_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: score_job_application
        {
            let url = BASE_URL + `/api/v1/job-applications/${application_id}/score`;
            let request = http.post(url);

            check(request, {
                "Scored": (r) => r.status === 200
            });
        }
    });

    group("/api/webhooks/paddle/subscription", () => {

        // Request No. 1: paddle_subscription_webhook
        {
            let url = BASE_URL + `/api/webhooks/paddle/subscription`;
            let request = http.post(url);

            check(request, {
                "Webhook processed": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/stock-movements/{movement_id}", () => {
        let movementId = 'TODO_EDIT_THE_MOVEMENT_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_stock_movement
        {
            let url = BASE_URL + `/api/v1/stock-movements/${movement_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/boms/{bom_id}", () => {
        let bomId = 'TODO_EDIT_THE_BOM_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_bom
        {
            let url = BASE_URL + `/api/v1/boms/${bom_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_bom
        {
            let url = BASE_URL + `/api/v1/boms/${bom_id}`;
            // TODO: edit the parameters of the request body.
            let body = {"components": "oas_any_type_not_mapped", "description": "string", "name": "string", "outputQuantity": "long", "productId": "uuid", "status": "bomstatus"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_bom
        {
            let url = BASE_URL + `/api/v1/boms/${bom_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/time-entries/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: clock_out_time_entry
        {
            let url = BASE_URL + `/api/v1/time-entries/${id}`;
            // TODO: edit the parameters of the request body.
            let body = {"clockOut": "date", "hours": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.patch(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/institute-profile", () => {

        // Request No. 1: get_institute_profile
        {
            let url = BASE_URL + `/api/v1/institute-profile`;
            let request = http.get(url);

            check(request, {
                "Institute profile": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_institute_profile
        {
            let url = BASE_URL + `/api/v1/institute-profile`;
            // TODO: edit the parameters of the request body.
            let body = {"instituteType": "string", "kapitalmarktorientiert": "boolean"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "Updated institute profile": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/activities/{activity_id}/status", () => {
        let activityId = 'TODO_EDIT_THE_ACTIVITY_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: update_activity_status
        {
            let url = BASE_URL + `/api/v1/activities/${activity_id}/status`;
            // TODO: edit the parameters of the request body.
            let body = {"status": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/service-assignments/", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let includeDeleted = 'TODO_EDIT_THE_INCLUDE_DELETED'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = '1'; // extracted from 'example' field defined at the parameter level of OpenAPI spec

        // Request No. 1: get_service_assignments
        {
            let url = BASE_URL + `/api/v1/service-assignments/?page=${page}&page_size=${page_size}&search=${search}&include_deleted=${include_deleted}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/compliance-trainings/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_compliance_training
        {
            let url = BASE_URL + `/api/v1/compliance-trainings/${id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_compliance_training
        {
            let url = BASE_URL + `/api/v1/compliance-trainings/${id}`;
            // TODO: edit the parameters of the request body.
            let body = {"assignable": "boolean", "code": "string", "description": "string", "passScore": "integer", "pluginPlatform": "string", "source": "trainingsource", "title": "string", "validityMonths": "integer"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_compliance_training
        {
            let url = BASE_URL + `/api/v1/compliance-trainings/${id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/invoices/{id}/xrechnung", () => {
        let supplierName = 'TODO_EDIT_THE_SUPPLIER_NAME'; // specify value as there is no example value for this parameter in OpenAPI spec
        let supplierStreet = 'TODO_EDIT_THE_SUPPLIER_STREET'; // specify value as there is no example value for this parameter in OpenAPI spec
        let supplierCountry = 'TODO_EDIT_THE_SUPPLIER_COUNTRY'; // specify value as there is no example value for this parameter in OpenAPI spec
        let supplierCity = 'TODO_EDIT_THE_SUPPLIER_CITY'; // specify value as there is no example value for this parameter in OpenAPI spec
        let supplierVatId = 'TODO_EDIT_THE_SUPPLIER_VAT_ID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let supplierZip = 'TODO_EDIT_THE_SUPPLIER_ZIP'; // specify value as there is no example value for this parameter in OpenAPI spec
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: generate_xrechnung_api
        {
            let url = BASE_URL + `/api/v1/invoices/${id}/xrechnung?supplier_name=${supplier_name}&supplier_street=${supplier_street}&supplier_city=${supplier_city}&supplier_zip=${supplier_zip}&supplier_country=${supplier_country}&supplier_vat_id=${supplier_vat_id}`;
            let request = http.get(url);

            check(request, {
                "XRechnung XML": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/goods-receipts/", () => {
        let supplierName = 'TODO_EDIT_THE_SUPPLIER_NAME'; // specify value as there is no example value for this parameter in OpenAPI spec
        let warehouseId = 'TODO_EDIT_THE_WAREHOUSE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let purchaseOrderId = 'TODO_EDIT_THE_PURCHASE_ORDER_ID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_goods_receipts
        {
            let url = BASE_URL + `/api/v1/goods-receipts/?page=${page}&page_size=${page_size}&purchase_order_id=${purchase_order_id}&supplier_name=${supplier_name}&warehouse_id=${warehouse_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/returns/", () => {
        let orderNumber = 'TODO_EDIT_THE_ORDER_NUMBER'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let customerName = 'TODO_EDIT_THE_CUSTOMER_NAME'; // specify value as there is no example value for this parameter in OpenAPI spec
        let status = 'TODO_EDIT_THE_STATUS'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_return_orders
        {
            let url = BASE_URL + `/api/v1/returns/?page=${page}&page_size=${page_size}&status=${status}&customer_name=${customer_name}&order_number=${order_number}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/user/tenants", () => {

        // Request No. 1: user_tenants
        {
            let url = BASE_URL + `/user/tenants`;
            let request = http.get(url);

            check(request, {
                "List of tenants": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/customers/", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let includeDeleted = 'TODO_EDIT_THE_INCLUDE_DELETED'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = '1'; // extracted from 'example' field defined at the parameter level of OpenAPI spec

        // Request No. 1: get_customers
        {
            let url = BASE_URL + `/api/v1/customers/?page=${page}&page_size=${page_size}&search=${search}&include_deleted=${include_deleted}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/auth/reset-password", () => {

        // Request No. 1: reset_password
        {
            let url = BASE_URL + `/auth/reset-password`;
            // TODO: edit the parameters of the request body.
            let body = {"newPassword": "string", "token": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Password reset successfully": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/reports/bilanz", () => {
        let month = 'TODO_EDIT_THE_MONTH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec
        let dateTo = 'TODO_EDIT_THE_DATE_TO'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let dateFrom = 'TODO_EDIT_THE_DATE_FROM'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: bilanz_report_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/reports/bilanz?year=${year}&month=${month}&date_from=${date_from}&date_to=${date_to}&page=${page}&page_size=${page_size}`;
            let request = http.get(url);

            check(request, {
                "Balance sheet": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/payment-gateways/oauth/authorize", () => {

        // Request No. 1: oauth_authorize_api
        {
            let url = BASE_URL + `/api/v1/payment-gateways/oauth/authorize`;
            // TODO: edit the parameters of the request body.
            let body = {"gatewayType": "string", "redirectUri": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "OAuth authorization URL": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/inventory-counts/generate", () => {

        // Request No. 1: generate_inventory_count
        {
            let url = BASE_URL + `/api/v1/inventory-counts/generate`;
            // TODO: edit the parameters of the request body.
            let body = {"notes": "string", "productIds": "list", "warehouseId": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/group-figures/{year}", () => {
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_group_figure
        {
            let url = BASE_URL + `/api/v1/group-figures/${year}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_group_figure
        {
            let url = BASE_URL + `/api/v1/group-figures/${year}`;
            // TODO: edit the parameters of the request body.
            let body = {"bilanzsumme": "string", "exemptionClaimed": "boolean", "mitarbeiter": "long", "nettoUmsatz": "string", "parentName": "string", "parentSitus": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_group_figure
        {
            let url = BASE_URL + `/api/v1/group-figures/${year}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/recurring-templates/{template_id}", () => {
        let templateId = 'TODO_EDIT_THE_TEMPLATE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_recurring_template
        {
            let url = BASE_URL + `/api/v1/recurring-templates/${template_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: delete_recurring_template
        {
            let url = BASE_URL + `/api/v1/recurring-templates/${template_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/bookkeeping/inventory-value", () => {

        // Request No. 1: get_inventory_value_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/inventory-value`;
            let request = http.get(url);

            check(request, {
                "Inventory value history": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/shop/editor", () => {

        // Request No. 1: shop_editor_save
        {
            let url = BASE_URL + `/api/v1/shop/editor`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, params);

            check(request, {
                "": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/reports/guv", () => {
        let month = 'TODO_EDIT_THE_MONTH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec
        let dateTo = 'TODO_EDIT_THE_DATE_TO'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let dateFrom = 'TODO_EDIT_THE_DATE_FROM'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: guv_report_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/reports/guv?year=${year}&month=${month}&date_from=${date_from}&date_to=${date_to}&page=${page}&page_size=${page_size}`;
            let request = http.get(url);

            check(request, {
                "GuV report": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/customer-groups", () => {

        // Request No. 1: create_customer_group
        {
            let url = BASE_URL + `/api/v1/customer-groups`;
            // TODO: edit the parameters of the request body.
            let body = {"description": "string", "memberIds": "list", "membershipFilter": "string", "name": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/invoices/{id}/pdf", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: download_invoice_pdf
        {
            let url = BASE_URL + `/api/v1/invoices/${id}/pdf`;
            let request = http.get(url);

            check(request, {
                "PDF file": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/workflows", () => {

        // Request No. 1: list_workflows_api
        {
            let url = BASE_URL + `/api/v1/workflows`;
            let request = http.get(url);

            check(request, {
                "Workflows (seeded with defaults on first access)": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/group-figures", () => {

        // Request No. 1: create_group_figure
        {
            let url = BASE_URL + `/api/v1/group-figures`;
            // TODO: edit the parameters of the request body.
            let body = {"bilanzsumme": "string", "exemptionClaimed": "boolean", "mitarbeiter": "long", "nettoUmsatz": "string", "parentName": "string", "parentSitus": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/attachments", () => {

        // Request No. 1: create_attachment
        {
            let url = BASE_URL + `/api/v1/attachments`;
            // TODO: edit the parameters of the request body.
            let body = {"contactId": "string", "fileName": "string", "fileSize": "long", "mimeType": "string", "originalName": "string", "pdfaPath": "string", "sha256Hash": "string", "uploadedBy": "uuid"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/contacts/sales-volume", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let contactType = 'TODO_EDIT_THE_CONTACT_TYPE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let tag = 'TODO_EDIT_THE_TAG'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: sales_volume
        {
            let url = BASE_URL + `/api/v1/contacts/sales-volume?page=${page}&page_size=${page_size}&search=${search}&contact_type=${contact_type}&tag=${tag}`;
            let request = http.get(url);

            check(request, {
                "Sales volume report": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/gewerbesteuer", () => {
        let country = 'TODO_EDIT_THE_COUNTRY'; // specify value as there is no example value for this parameter in OpenAPI spec
        let gewerbeertrag = 'TODO_EDIT_THE_GEWERBEERTRAG'; // specify value as there is no example value for this parameter in OpenAPI spec
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec
        let gemeindeschluessel = 'TODO_EDIT_THE_GEMEINDESCHLUESSEL'; // specify value as there is no example value for this parameter in OpenAPI spec
        let hebesatz = 'TODO_EDIT_THE_HEBESATZ'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: gewerbesteuer_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/gewerbesteuer?year=${year}&hebesatz=${hebesatz}&gewerbeertrag=${gewerbeertrag}&country=${country}&gemeindeschluessel=${gemeindeschluessel}`;
            let request = http.get(url);

            check(request, {
                "Gewerbesteuer / Trade Tax Ergebnis": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/banking/transactions", () => {

        // Request No. 1: bank_transactions_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/banking/transactions`;
            let request = http.get(url);

            check(request, {
                "Bank-Transaktionen": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/proforma-invoices", () => {

        // Request No. 1: create_proforma_invoice
        {
            let url = BASE_URL + `/api/v1/proforma-invoices`;
            // TODO: edit the parameters of the request body.
            let body = {"convertedAt": "date", "convertedToInvoiceId": "string", "currency": "currencycode", "customerId": "string", "customerSnapshot": "oas_any_type_not_mapped", "issueDate": "date", "lineItems": "oas_any_type_not_mapped", "notes": "string", "orderNumber": "string", "paymentDueDate": "date", "quotationId": "string", "status": "proformainvoicestatus", "subtotal": "string", "totalAmount": "string", "totalTax": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/support/leads", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let source = 'TODO_EDIT_THE_SOURCE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let status = 'TODO_EDIT_THE_STATUS'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_leads_api
        {
            let url = BASE_URL + `/api/v1/support/leads?status=${status}&source=${source}&search=${search}&page=${page}&page_size=${page_size}`;
            let request = http.get(url);

            check(request, {
                "Leads list": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/ebilanz/xbrl", () => {
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec
        let dateTo = 'TODO_EDIT_THE_DATE_TO'; // specify value as there is no example value for this parameter in OpenAPI spec
        let dateFrom = 'TODO_EDIT_THE_DATE_FROM'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: ebilanz_xbrl_export_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/ebilanz/xbrl?year=${year}&date_from=${date_from}&date_to=${date_to}`;
            let request = http.get(url);

            check(request, {
                "XBRL XML content": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/legal/documents/reset", () => {

        // Request No. 1: reset_legal_documents
        {
            let url = BASE_URL + `/api/v1/legal/documents/reset`;
            // TODO: edit the parameters of the request body.
            let body = {"docType": "string", "lang": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Reset legal documents": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/orders/{order_id}", () => {
        let orderId = 'TODO_EDIT_THE_ORDER_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: patch_order
        {
            let url = BASE_URL + `/api/v1/orders/${order_id}`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.patch(url, params);

            check(request, {
                "Order updated": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/emissions/targets", () => {

        // Request No. 1: emissions_targets_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/emissions/targets`;
            let request = http.get(url);

            check(request, {
                "Emission reduction targets (ESRS E1-4)": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: create_emission_target_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/emissions/targets`;
            // TODO: edit the parameters of the request body.
            let body = {"baseValue": "string", "baseYear": "integer", "description": "string", "scope": "string", "targetValue": "string", "targetYear": "integer"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Emission target created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/warehouses/{warehouse_id}/stock", () => {
        let warehouseId = 'TODO_EDIT_THE_WAREHOUSE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_warehouse_stock
        {
            let url = BASE_URL + `/api/v1/warehouses/${warehouse_id}/stock`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: create_warehouse_stock
        {
            let url = BASE_URL + `/api/v1/warehouses/${warehouse_id}/stock`;
            // TODO: edit the parameters of the request body.
            let body = {"batchNumber": "string", "binLocation": "string", "expiryDate": "date", "productId": "uuid", "quantity": "long", "serialNumbers": "list"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/bookkeeping/cashflow", () => {
        let month = 'TODO_EDIT_THE_MONTH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_cashflow
        {
            let url = BASE_URL + `/api/v1/bookkeeping/cashflow?year=${year}&month=${month}`;
            let request = http.get(url);

            check(request, {
                "Cashflow report": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/stock-transfers/", () => {
        let warehouseId = 'TODO_EDIT_THE_WAREHOUSE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let status = 'TODO_EDIT_THE_STATUS'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_stock_transfers
        {
            let url = BASE_URL + `/api/v1/stock-transfers/?page=${page}&page_size=${page_size}&status=${status}&warehouse_id=${warehouse_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/verfahrensdokumentation", () => {

        // Request No. 1: get_verfahrensdokumentation
        {
            let url = BASE_URL + `/api/v1/bookkeeping/verfahrensdokumentation`;
            let request = http.get(url);

            check(request, {
                "Verfahrensdokumentation": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/contacts/{contact_id}", () => {
        let contactId = 'TODO_EDIT_THE_CONTACT_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_contact
        {
            let url = BASE_URL + `/api/v1/contacts/${contact_id}`;
            let request = http.get(url);

            check(request, {
                "Contact": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_contact
        {
            let url = BASE_URL + `/api/v1/contacts/${contact_id}`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, params);

            check(request, {
                "Updated": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_contact
        {
            let url = BASE_URL + `/api/v1/contacts/${contact_id}`;
            let request = http.del(url);

            check(request, {
                "Deleted": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/service-jobs", () => {

        // Request No. 1: create_service_job
        {
            let url = BASE_URL + `/api/v1/service-jobs`;
            // TODO: edit the parameters of the request body.
            let body = {"address": "string", "customerEmail": "string", "customerId": "uuid", "customerName": "string", "customerPhone": "string", "description": "string", "estimatedDurationMinutes": "integer", "lat": "double", "lng": "double", "notes": "string", "status": "servicejobstatus"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/product-categories/{category_id}", () => {
        let categoryId = 'TODO_EDIT_THE_CATEGORY_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_product_category
        {
            let url = BASE_URL + `/api/v1/product-categories/${category_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_product_category
        {
            let url = BASE_URL + `/api/v1/product-categories/${category_id}`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, params);

            check(request, {
                "Updated": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_product_category
        {
            let url = BASE_URL + `/api/v1/product-categories/${category_id}`;
            let request = http.del(url);

            check(request, {
                "Deleted": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/job-applications/{application_id}/cv", () => {
        let applicationId = 'TODO_EDIT_THE_APPLICATION_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: download_cv
        {
            let url = BASE_URL + `/api/v1/job-applications/${application_id}/cv`;
            let request = http.get(url);

            check(request, {
                "CV file": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/shipping/credentials", () => {

        // Request No. 1: get_credentials_api
        {
            let url = BASE_URL + `/api/v1/shipping/credentials`;
            let request = http.get(url);

            check(request, {
                "Configured shipping provider credentials": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: save_credentials_api
        {
            let url = BASE_URL + `/api/v1/shipping/credentials`;
            // TODO: edit the parameters of the request body.
            let body = {"dhl": {"apiKey": "string", "clientId": "string", "clientSecret": "string"}, "ups": {"clientId": "string", "clientSecret": "string", "shipperNumber": "string"}};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "Saved shipping provider credentials": (r) => r.status === 200
            });
        }
    });

    group("/user/invite", () => {

        // Request No. 1: invite_user
        {
            let url = BASE_URL + `/user/invite`;
            // TODO: edit the parameters of the request body.
            let body = {"email": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Invitation sent": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/import/{job_id}", () => {
        let jobId = 'TODO_EDIT_THE_JOB_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_import_status
        {
            let url = BASE_URL + `/api/v1/import/${job_id}`;
            let request = http.get(url);

            check(request, {
                "Import job status": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/supplier-invoices/", () => {
        let supplierName = 'TODO_EDIT_THE_SUPPLIER_NAME'; // specify value as there is no example value for this parameter in OpenAPI spec
        let purchaseOrderId = 'TODO_EDIT_THE_PURCHASE_ORDER_ID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let status = 'TODO_EDIT_THE_STATUS'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_supplier_invoices
        {
            let url = BASE_URL + `/api/v1/supplier-invoices/?page=${page}&page_size=${page_size}&status=${status}&purchase_order_id=${purchase_order_id}&supplier_name=${supplier_name}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/euer", () => {
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: euer_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/euer?year=${year}`;
            let request = http.get(url);

            check(request, {
                "EÜR Ergebnis": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/job-postings", () => {
        let pageSize = 'TODO_EDIT_THE_PAGESIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let status = 'TODO_EDIT_THE_STATUS'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_job_postings
        {
            let url = BASE_URL + `/api/v1/job-postings?status=${status}&page=${page}&pageSize=${pageSize}`;
            let request = http.get(url);

            check(request, {
                "Job postings list": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: create_job_posting
        {
            let url = BASE_URL + `/api/v1/job-postings`;
            // TODO: edit the parameters of the request body.
            let body = {"currency": "string", "department": "string", "description": "string", "employmentType": "employmenttype", "location": "string", "remote": "boolean", "requiredSkills": "oas_any_type_not_mapped", "requirements": "string", "salaryMax": "integer", "salaryMin": "integer", "status": "jobpostingstatus", "title": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/production-orders/{production_order_id}", () => {
        let productionOrderId = 'TODO_EDIT_THE_PRODUCTION_ORDER_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_production_order
        {
            let url = BASE_URL + `/api/v1/production-orders/${production_order_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_production_order
        {
            let url = BASE_URL + `/api/v1/production-orders/${production_order_id}`;
            // TODO: edit the parameters of the request body.
            let body = {"bomId": "uuid", "components": "oas_any_type_not_mapped", "endDate": "date", "notes": "string", "orderNumber": "string", "productId": "uuid", "quantity": "long", "sourceWarehouseId": "string", "startDate": "date", "status": "productionorderstatus", "targetWarehouseId": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_production_order
        {
            let url = BASE_URL + `/api/v1/production-orders/${production_order_id}`;
            let request = http.del(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/proforma-invoices/", () => {
        let orderNumber = 'TODO_EDIT_THE_ORDER_NUMBER'; // specify value as there is no example value for this parameter in OpenAPI spec
        let customerId = 'TODO_EDIT_THE_CUSTOMER_ID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let status = 'TODO_EDIT_THE_STATUS'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_proforma_invoices
        {
            let url = BASE_URL + `/api/v1/proforma-invoices/?page=${page}&page_size=${page_size}&status=${status}&customer_id=${customer_id}&order_number=${order_number}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/inventory-value/record", () => {

        // Request No. 1: record_inventory_value_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/inventory-value/record`;
            let request = http.post(url);

            check(request, {
                "Current value snapshot recorded": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/oss", () => {

        // Request No. 1: oss_report_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/oss`;
            let request = http.get(url);

            check(request, {
                "OSS dependencies": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/sepa-direct-debit", () => {
        let debtorBic = 'TODO_EDIT_THE_DEBTOR_BIC'; // specify value as there is no example value for this parameter in OpenAPI spec
        let creditorIban = 'TODO_EDIT_THE_CREDITOR_IBAN'; // specify value as there is no example value for this parameter in OpenAPI spec
        let amount = 'TODO_EDIT_THE_AMOUNT'; // specify value as there is no example value for this parameter in OpenAPI spec
        let creditorName = 'TODO_EDIT_THE_CREDITOR_NAME'; // specify value as there is no example value for this parameter in OpenAPI spec
        let debtorIban = 'TODO_EDIT_THE_DEBTOR_IBAN'; // specify value as there is no example value for this parameter in OpenAPI spec
        let mandateId = 'TODO_EDIT_THE_MANDATE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let mandateDate = 'TODO_EDIT_THE_MANDATE_DATE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let description = 'TODO_EDIT_THE_DESCRIPTION'; // specify value as there is no example value for this parameter in OpenAPI spec
        let debtorName = 'TODO_EDIT_THE_DEBTOR_NAME'; // specify value as there is no example value for this parameter in OpenAPI spec
        let creditorBic = 'TODO_EDIT_THE_CREDITOR_BIC'; // specify value as there is no example value for this parameter in OpenAPI spec
        let collectionDate = 'TODO_EDIT_THE_COLLECTION_DATE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let creditorId = 'TODO_EDIT_THE_CREDITOR_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: create_sepa_direct_debit_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/sepa-direct-debit?creditor_name=${creditor_name}&creditor_iban=${creditor_iban}&creditor_bic=${creditor_bic}&creditor_id=${creditor_id}&mandate_id=${mandate_id}&mandate_date=${mandate_date}&debtor_name=${debtor_name}&debtor_iban=${debtor_iban}&debtor_bic=${debtor_bic}&amount=${amount}&collection_date=${collection_date}&description=${description}`;
            let request = http.post(url);

            check(request, {
                "SEPA Direct Debit XML": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/inventory-counts/{inventory_count_id}/status", () => {
        let inventoryCountId = 'TODO_EDIT_THE_INVENTORY_COUNT_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: update_inventory_count_status
        {
            let url = BASE_URL + `/api/v1/inventory-counts/${inventory_count_id}/status`;
            // TODO: edit the parameters of the request body.
            let body = {"status": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/pos/tables/{id}/free", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: pos_free_table
        {
            let url = BASE_URL + `/api/pos/tables/${id}/free`;
            let request = http.post(url);

            check(request, {
                "": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/orders/{order_number}/shipments", () => {
        let orderNumber = 'TODO_EDIT_THE_ORDER_NUMBER'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: create_shipment_from_order
        {
            let url = BASE_URL + `/api/v1/orders/${order_number}/shipments`;
            // TODO: edit the parameters of the request body.
            let body = {"carrier": "string", "service": "string", "weightKg": "double"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Shipment created via carrier API": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/product-variants", () => {

        // Request No. 1: create_product_variant
        {
            let url = BASE_URL + `/api/v1/product-variants`;
            // TODO: edit the parameters of the request body.
            let body = {"barcode": "string", "imageLink": "string", "isActive": "boolean", "name": "string", "optionValues": "oas_any_type_not_mapped", "price": "string", "priceDelta": "string", "productId": "uuid", "sku": "string", "stockQuantity": "long"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/inventory-counts", () => {

        // Request No. 1: create_inventory_count
        {
            let url = BASE_URL + `/api/v1/inventory-counts`;
            // TODO: edit the parameters of the request body.
            let body = {"countDate": "date", "countNumber": "string", "lineItems": "oas_any_type_not_mapped", "notes": "string", "status": "inventorycountstatus", "warehouseId": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/marketplace/webhook/{platform}/{connection_id}", () => {
        let connectionId = 'TODO_EDIT_THE_CONNECTION_ID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let platform = 'TODO_EDIT_THE_PLATFORM'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: webhook_receiver_api
        {
            let url = BASE_URL + `/api/v1/marketplace/webhook/${platform}/${connection_id}`;
            let request = http.post(url);

            check(request, {
                "Webhook received": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/gdpr/export/{contact_id}", () => {
        let contactId = 'TODO_EDIT_THE_CONTACT_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: export_contact_data
        {
            let url = BASE_URL + `/api/v1/gdpr/export/${contact_id}`;
            let request = http.get(url);

            check(request, {
                "Art. 15 data-subject export": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/participations", () => {

        // Request No. 1: create_participation
        {
            let url = BASE_URL + `/api/v1/participations`;
            // TODO: edit the parameters of the request body.
            let body = {"acquiredAt": "date", "boardAppointment": "boolean", "companyName": "string", "controlAgreement": "boolean", "legalForm": "string", "ownershipPct": "string", "purposeVehicle": "boolean", "votingMajority": "boolean"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/credit-notes/{credit_note_id}/pdf", () => {
        let creditNoteId = 'TODO_EDIT_THE_CREDIT_NOTE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: download_credit_note_pdf
        {
            let url = BASE_URL + `/api/v1/credit-notes/${credit_note_id}/pdf`;
            let request = http.get(url);

            check(request, {
                "PDF file": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/shareholders/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_shareholder
        {
            let url = BASE_URL + `/api/v1/shareholders/${id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_shareholder
        {
            let url = BASE_URL + `/api/v1/shareholders/${id}`;
            // TODO: edit the parameters of the request body.
            let body = {"address": "string", "birthDate": "date", "email": "string", "firstName": "string", "lastName": "string", "shareNumber": "string", "shares": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_shareholder
        {
            let url = BASE_URL + `/api/v1/shareholders/${id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/shipments/{shipment_id}", () => {
        let shipmentId = 'TODO_EDIT_THE_SHIPMENT_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_shipment
        {
            let url = BASE_URL + `/api/v1/shipments/${shipment_id}`;
            let request = http.get(url);

            check(request, {
                "Shipment found": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: delete_shipment
        {
            let url = BASE_URL + `/api/v1/shipments/${shipment_id}`;
            let request = http.del(url);

            check(request, {
                "Shipment deleted": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/email-templates", () => {

        // Request No. 1: create_email_template
        {
            let url = BASE_URL + `/api/v1/email-templates`;
            // TODO: edit the parameters of the request body.
            let body = {"body": "string", "name": "string", "status": "emailtemplatestatus", "subject": "string", "variables": "oas_any_type_not_mapped"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/shipping-thresholds/", () => {
        let productId = 'TODO_EDIT_THE_PRODUCT_ID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let warehouseId = 'TODO_EDIT_THE_WAREHOUSE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let isActive = 'TODO_EDIT_THE_IS_ACTIVE'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_shipping_thresholds
        {
            let url = BASE_URL + `/api/v1/shipping-thresholds/?page=${page}&page_size=${page_size}&product_id=${product_id}&warehouse_id=${warehouse_id}&is_active=${is_active}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/kyc-records", () => {

        // Request No. 1: create_kyc_record
        {
            let url = BASE_URL + `/api/v1/kyc-records`;
            // TODO: edit the parameters of the request body.
            let body = {"customerId": "string", "customerName": "string", "kycDate": "date", "notes": "string", "retentionUntil": "date", "riskAssessment": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/packing/{order_number}/print-label", () => {
        let orderNumber = 'TODO_EDIT_THE_ORDER_NUMBER'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: print_label
        {
            let url = BASE_URL + `/api/v1/packing/${order_number}/print-label`;
            let request = http.post(url);

            check(request, {
                "Label printed": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/product-attributes/{attribute_id}", () => {
        let attributeId = 'TODO_EDIT_THE_ATTRIBUTE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_product_attribute
        {
            let url = BASE_URL + `/api/v1/product-attributes/${attribute_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_product_attribute
        {
            let url = BASE_URL + `/api/v1/product-attributes/${attribute_id}`;
            // TODO: edit the parameters of the request body.
            let body = {"isFilterable": "boolean", "name": "string", "position": "integer", "productId": "uuid", "unit": "string", "value": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_product_attribute
        {
            let url = BASE_URL + `/api/v1/product-attributes/${attribute_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/payroll/{id}/approve", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: payroll_approve
        {
            let url = BASE_URL + `/api/v1/payroll/${id}/approve`;
            let request = http.post(url);

            check(request, {
                "": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/emissions/entries/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: delete_emission_entry_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/emissions/entries/${id}`;
            let request = http.del(url);

            check(request, {
                "Emission entry deleted": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/time-entries", () => {
        let active = 'TODO_EDIT_THE_ACTIVE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let from = 'TODO_EDIT_THE_FROM'; // specify value as there is no example value for this parameter in OpenAPI spec
        let employeeId = 'TODO_EDIT_THE_EMPLOYEE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let to = 'TODO_EDIT_THE_TO'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_time_entries
        {
            let url = BASE_URL + `/api/v1/time-entries?from=${from}&to=${to}&active=${active}&employee_id=${employee_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: clock_in_time_entry
        {
            let url = BASE_URL + `/api/v1/time-entries`;
            // TODO: edit the parameters of the request body.
            let body = {"notes": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/marketplace/connections/{connection_id}/sync", () => {
        let connectionId = 'TODO_EDIT_THE_CONNECTION_ID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let syncType = 'TODO_EDIT_THE_SYNC_TYPE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let direction = 'TODO_EDIT_THE_DIRECTION'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: trigger_sync_api
        {
            let url = BASE_URL + `/api/v1/marketplace/connections/${connection_id}/sync?sync_type=${sync_type}&direction=${direction}`;
            let request = http.post(url);

            check(request, {
                "Sync triggered": (r) => r.status === 200
            });
        }
    });

    group("/auth/verify-email", () => {

        // Request No. 1: verify_email
        {
            let url = BASE_URL + `/auth/verify-email`;
            // TODO: edit the parameters of the request body.
            let body = {"token": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Email verified successfully": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/support/ai/workers/{worker_id}/run", () => {
        let workerId = 'TODO_EDIT_THE_WORKER_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: run_worker_api
        {
            let url = BASE_URL + `/api/v1/support/ai/workers/${worker_id}/run`;
            // TODO: edit the parameters of the request body.
            let body = {"instructions": "string", "messageBody": "string", "ticketId": "uuid"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Worker executed": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/quotations/{quotation_id}", () => {
        let quotationId = 'TODO_EDIT_THE_QUOTATION_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_quotation
        {
            let url = BASE_URL + `/api/v1/quotations/${quotation_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_quotation
        {
            let url = BASE_URL + `/api/v1/quotations/${quotation_id}`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_quotation
        {
            let url = BASE_URL + `/api/v1/quotations/${quotation_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/bookkeeping/gez", () => {
        let jahr = 'TODO_EDIT_THE_JAHR'; // specify value as there is no example value for this parameter in OpenAPI spec
        let beschaefigte = 'TODO_EDIT_THE_BESCHAEFIGTE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let betriebsstaetten = 'TODO_EDIT_THE_BETRIEBSSTAETTEN'; // specify value as there is no example value for this parameter in OpenAPI spec
        let hotelzimmer = 'TODO_EDIT_THE_HOTELZIMMER'; // specify value as there is no example value for this parameter in OpenAPI spec
        let kfz = 'TODO_EDIT_THE_KFZ'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: gez_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/gez?jahr=${jahr}&betriebsstaetten=${betriebsstaetten}&kfz=${kfz}&hotelzimmer=${hotelzimmer}&beschaefigte=${beschaefigte}`;
            let request = http.get(url);

            check(request, {
                "Rundfunkbeitrag (GEZ) Berechnung nach § 5 RBStV": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/contacts", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let contactType = 'TODO_EDIT_THE_CONTACT_TYPE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let tag = 'TODO_EDIT_THE_TAG'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_contacts
        {
            let url = BASE_URL + `/api/v1/contacts?page=${page}&page_size=${page_size}&search=${search}&contact_type=${contact_type}&tag=${tag}`;
            let request = http.get(url);

            check(request, {
                "List of contacts": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: create_contact
        {
            let url = BASE_URL + `/api/v1/contacts`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/notifications/{id}/read", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: mark_as_read
        {
            let url = BASE_URL + `/api/v1/notifications/${id}/read`;
            let request = http.put(url);

            check(request, {
                "Marked as read": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/vouchers/", () => {
        let voucherType = 'TODO_EDIT_THE_VOUCHER_TYPE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let voucherStatus = 'TODO_EDIT_THE_VOUCHER_STATUS'; // specify value as there is no example value for this parameter in OpenAPI spec
        let contactName = 'TODO_EDIT_THE_CONTACT_NAME'; // specify value as there is no example value for this parameter in OpenAPI spec
        let dateTo = 'TODO_EDIT_THE_DATE_TO'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let dateFrom = 'TODO_EDIT_THE_DATE_FROM'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_vouchers
        {
            let url = BASE_URL + `/api/v1/vouchers/?page=${page}&page_size=${page_size}&voucher_type=${voucher_type}&voucher_status=${voucher_status}&contact_name=${contact_name}&date_from=${date_from}&date_to=${date_to}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/payment-gateways/", () => {

        // Request No. 1: list_payment_gateways_api
        {
            let url = BASE_URL + `/api/v1/payment-gateways/`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/delivery-notes/{delivery_note_id}", () => {
        let deliveryNoteId = 'TODO_EDIT_THE_DELIVERY_NOTE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_delivery_note
        {
            let url = BASE_URL + `/api/v1/delivery-notes/${delivery_note_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: delete_delivery_note
        {
            let url = BASE_URL + `/api/v1/delivery-notes/${delivery_note_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/returns/logistics-summary", () => {

        // Request No. 1: return_logistics_summary
        {
            let url = BASE_URL + `/api/v1/returns/logistics-summary`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/public/jobs/{posting_id}/apply", () => {
        let postingId = 'TODO_EDIT_THE_POSTING_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: apply_public
        {
            let url = BASE_URL + `/api/v1/public/jobs/${posting_id}/apply`;
            let request = http.post(url);

            check(request, {
                "Application received": (r) => r.status === 201
            });
        }
    });

    group("/auth/magic-link", () => {

        // Request No. 1: magic_link_login
        {
            let url = BASE_URL + `/auth/magic-link`;
            // TODO: edit the parameters of the request body.
            let body = {"email": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Magic link email sent if the account exists": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/declarations/", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let includeDeleted = 'TODO_EDIT_THE_INCLUDE_DELETED'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = '1'; // extracted from 'example' field defined at the parameter level of OpenAPI spec

        // Request No. 1: get_declarations
        {
            let url = BASE_URL + `/api/v1/declarations/?page=${page}&page_size=${page_size}&search=${search}&include_deleted=${include_deleted}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/posting-categories/{category_id}", () => {
        let categoryId = 'TODO_EDIT_THE_CATEGORY_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: update_posting_category
        {
            let url = BASE_URL + `/api/v1/posting-categories/${category_id}`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, params);

            check(request, {
                "Updated": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: delete_posting_category
        {
            let url = BASE_URL + `/api/v1/posting-categories/${category_id}`;
            let request = http.del(url);

            check(request, {
                "Deleted": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/marketplace/connections/{connection_id}/logs", () => {
        let connectionId = 'TODO_EDIT_THE_CONNECTION_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_sync_logs_api
        {
            let url = BASE_URL + `/api/v1/marketplace/connections/${connection_id}/logs`;
            let request = http.get(url);

            check(request, {
                "Sync logs": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/rfqs/{rfq_id}/convert", () => {
        let rfqId = 'TODO_EDIT_THE_RFQ_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: convert_rfq
        {
            let url = BASE_URL + `/api/v1/rfqs/${rfq_id}/convert`;
            let request = http.post(url);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/credit-notes/{credit_note_id}", () => {
        let creditNoteId = 'TODO_EDIT_THE_CREDIT_NOTE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_credit_note
        {
            let url = BASE_URL + `/api/v1/credit-notes/${credit_note_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/pos/kasse/pay-in-out", () => {

        // Request No. 1: pos_kasse_pay_in_out
        {
            let url = BASE_URL + `/api/pos/kasse/pay-in-out`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, params);

            check(request, {
                "": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/import/start", () => {

        // Request No. 1: start_import
        {
            let url = BASE_URL + `/api/v1/import/start`;
            // TODO: edit the parameters of the request body.
            let body = {"apiKey": "string", "provider": "string", "years": "list"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Import job queued": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/stock-transfers/{stock_transfer_id}", () => {
        let stockTransferId = 'TODO_EDIT_THE_STOCK_TRANSFER_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_stock_transfer
        {
            let url = BASE_URL + `/api/v1/stock-transfers/${stock_transfer_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: delete_stock_transfer
        {
            let url = BASE_URL + `/api/v1/stock-transfers/${stock_transfer_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/returns/{return_order_id}", () => {
        let returnOrderId = 'TODO_EDIT_THE_RETURN_ORDER_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_return_order
        {
            let url = BASE_URL + `/api/v1/returns/${return_order_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_return_order
        {
            let url = BASE_URL + `/api/v1/returns/${return_order_id}`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_return_order
        {
            let url = BASE_URL + `/api/v1/returns/${return_order_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/attachments/{attachment_id}/versions/{version_id}/restore", () => {
        let versionId = 'TODO_EDIT_THE_VERSION_ID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let attachmentId = 'TODO_EDIT_THE_ATTACHMENT_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: restore_attachment_version
        {
            let url = BASE_URL + `/api/v1/attachments/${attachment_id}/versions/${version_id}/restore`;
            let request = http.post(url);

            check(request, {
                "Attachment restored": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/reports/umsatzsteuer", () => {
        let month = 'TODO_EDIT_THE_MONTH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec
        let dateTo = 'TODO_EDIT_THE_DATE_TO'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let dateFrom = 'TODO_EDIT_THE_DATE_FROM'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: umsatzsteuer_report_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/reports/umsatzsteuer?year=${year}&month=${month}&date_from=${date_from}&date_to=${date_to}&page=${page}&page_size=${page_size}`;
            let request = http.get(url);

            check(request, {
                "VAT report": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/quotations/{quotation_id}/pursue", () => {
        let quotationId = 'TODO_EDIT_THE_QUOTATION_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: pursue_quotation
        {
            let url = BASE_URL + `/api/v1/quotations/${quotation_id}/pursue`;
            let request = http.post(url);

            check(request, {
                "Created OrderConfirmation": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/supplier-conditions/", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let supplierContactId = 'TODO_EDIT_THE_SUPPLIER_CONTACT_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_supplier_conditions
        {
            let url = BASE_URL + `/api/v1/supplier-conditions/?page=${page}&page_size=${page_size}&supplier_contact_id=${supplier_contact_id}&search=${search}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/payments/open-invoices/{customer_id}", () => {
        let customerId = 'TODO_EDIT_THE_CUSTOMER_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_open_invoices_api
        {
            let url = BASE_URL + `/api/v1/payments/open-invoices/${customer_id}`;
            let request = http.get(url);

            check(request, {
                "Open invoices list": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/inventory-counts/", () => {
        let warehouseId = 'TODO_EDIT_THE_WAREHOUSE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let status = 'TODO_EDIT_THE_STATUS'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_inventory_counts
        {
            let url = BASE_URL + `/api/v1/inventory-counts/?page=${page}&page_size=${page_size}&status=${status}&warehouse_id=${warehouse_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/pos/tables", () => {

        // Request No. 1: pos_list_tables
        {
            let url = BASE_URL + `/api/pos/tables`;
            let request = http.get(url);

            check(request, {
                "": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: pos_create_table
        {
            let url = BASE_URL + `/api/pos/tables`;
            // TODO: edit the parameters of the request body.
            let body = {"currentOrderNumber": "string", "name": "string", "status": "postablestatus"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/rfqs/", () => {
        let supplierName = 'TODO_EDIT_THE_SUPPLIER_NAME'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let status = 'TODO_EDIT_THE_STATUS'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_rfqs
        {
            let url = BASE_URL + `/api/v1/rfqs/?page=${page}&page_size=${page_size}&status=${status}&supplier_name=${supplier_name}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/activities/{activity_id}", () => {
        let activityId = 'TODO_EDIT_THE_ACTIVITY_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_activity
        {
            let url = BASE_URL + `/api/v1/activities/${activity_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_activity
        {
            let url = BASE_URL + `/api/v1/activities/${activity_id}`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_activity
        {
            let url = BASE_URL + `/api/v1/activities/${activity_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/bookkeeping/emissions/targets/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: delete_emission_target_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/emissions/targets/${id}`;
            let request = http.del(url);

            check(request, {
                "Emission target deleted": (r) => r.status === 200
            });
        }
    });

    group("/api/pos/sumup/checkout", () => {

        // Request No. 1: pos_sumup_checkout
        {
            let url = BASE_URL + `/api/pos/sumup/checkout`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, params);

            check(request, {
                "": (r) => r.status === 200
            });
        }
    });

    group("/auth/login", () => {

        // Request No. 1: login
        {
            let url = BASE_URL + `/auth/login`;
            // TODO: edit the parameters of the request body.
            let body = {"email": "string", "password": "string", "totpCode": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Login successful, returns JWT tokens": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/event-subscriptions", () => {

        // Request No. 1: create_event_subscription
        {
            let url = BASE_URL + `/api/v1/event-subscriptions`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/silent-partners/", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let includeDeleted = 'TODO_EDIT_THE_INCLUDE_DELETED'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = '1'; // extracted from 'example' field defined at the parameter level of OpenAPI spec

        // Request No. 1: get_silent_partners
        {
            let url = BASE_URL + `/api/v1/silent-partners/?page=${page}&page_size=${page_size}&search=${search}&include_deleted=${include_deleted}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/shipping-rules", () => {

        // Request No. 1: create_shipping_rule
        {
            let url = BASE_URL + `/api/v1/shipping-rules`;
            // TODO: edit the parameters of the request body.
            let body = {"carrier": "string", "country": "countrycode", "deliveryTime": "string", "isActive": "boolean", "maxWeightKg": "double", "minWeightKg": "double", "name": "string", "notes": "string", "price": "string", "priority": "integer"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/price-tiers", () => {

        // Request No. 1: create_price_tier
        {
            let url = BASE_URL + `/api/v1/price-tiers`;
            // TODO: edit the parameters of the request body.
            let body = {"customerGroupId": "string", "minQuantity": "long", "productId": "uuid", "unitPrice": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/declarations", () => {

        // Request No. 1: create_declaration
        {
            let url = BASE_URL + `/api/v1/declarations`;
            // TODO: edit the parameters of the request body.
            let body = {"declarationType": "declarationtype", "isCurrent": "boolean", "text": "string", "validFrom": "date", "version": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/attachments/{attachment_id}/versions", () => {
        let attachmentId = 'TODO_EDIT_THE_ATTACHMENT_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_attachment_versions
        {
            let url = BASE_URL + `/api/v1/attachments/${attachment_id}/versions`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: create_attachment_version
        {
            let url = BASE_URL + `/api/v1/attachments/${attachment_id}/versions`;
            // TODO: edit the parameters of the request body.
            let body = {"fileName": "string", "fileSize": "long", "mimeType": "string", "originalName": "string", "sha256Hash": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "New current version recorded": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/service-assignments/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_service_assignment
        {
            let url = BASE_URL + `/api/v1/service-assignments/${id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_service_assignment
        {
            let url = BASE_URL + `/api/v1/service-assignments/${id}`;
            // TODO: edit the parameters of the request body.
            let body = {"employeeId": "uuid", "jobId": "uuid", "notes": "string", "scheduledDate": "date", "scheduledEnd": "string", "scheduledStart": "string", "status": "serviceassignmentstatus"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_service_assignment
        {
            let url = BASE_URL + `/api/v1/service-assignments/${id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/employees/", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let includeDeleted = 'TODO_EDIT_THE_INCLUDE_DELETED'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = '1'; // extracted from 'example' field defined at the parameter level of OpenAPI spec

        // Request No. 1: get_employees
        {
            let url = BASE_URL + `/api/v1/employees/?page=${page}&page_size=${page_size}&search=${search}&include_deleted=${include_deleted}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/shipping/providers", () => {

        // Request No. 1: list_providers_api
        {
            let url = BASE_URL + `/api/v1/shipping/providers`;
            let request = http.get(url);

            check(request, {
                "List of shipping providers": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/budgets/goals/{category}", () => {
        let category = 'TODO_EDIT_THE_CATEGORY'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: upsert_budget_goal_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/budgets/goals/${category}`;
            // TODO: edit the parameters of the request body.
            let body = {"monthlyGoal": "string", "year": "integer"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "Budget goal saved (upsert)": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/production-orders", () => {

        // Request No. 1: create_production_order
        {
            let url = BASE_URL + `/api/v1/production-orders`;
            // TODO: edit the parameters of the request body.
            let body = {"bomId": "uuid", "components": "oas_any_type_not_mapped", "endDate": "date", "notes": "string", "orderNumber": "string", "productId": "uuid", "quantity": "long", "sourceWarehouseId": "string", "startDate": "date", "status": "productionorderstatus", "targetWarehouseId": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/packing/queue", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_packing_queue
        {
            let url = BASE_URL + `/api/v1/packing/queue?page=${page}&page_size=${page_size}&search=${search}`;
            let request = http.get(url);

            check(request, {
                "Packing queue": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/gdpr/export", () => {

        // Request No. 1: export_gdpr
        {
            let url = BASE_URL + `/api/v1/gdpr/export`;
            let request = http.get(url);

            check(request, {
                "Personal data export": (r) => r.status === 200
            });
        }
    });

    group("/api/pos/products", () => {
        let q = 'TODO_EDIT_THE_Q'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: pos_list_products
        {
            let url = BASE_URL + `/api/pos/products?q=${q}`;
            let request = http.get(url);

            check(request, {
                "": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/me/permissions", () => {

        // Request No. 1: my_permissions
        {
            let url = BASE_URL + `/api/v1/me/permissions`;
            let request = http.get(url);

            check(request, {
                "": (r) => r.status === 200
            });
        }
    });

    group("/auth/forgot-password", () => {

        // Request No. 1: forgot_password
        {
            let url = BASE_URL + `/auth/forgot-password`;
            // TODO: edit the parameters of the request body.
            let body = {"email": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Password reset email sent if the account exists": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/notifications/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: delete_notification
        {
            let url = BASE_URL + `/api/v1/notifications/${id}`;
            let request = http.del(url);

            check(request, {
                "Deleted": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/workflows/{workflow_id}/enabled", () => {
        let workflowId = 'TODO_EDIT_THE_WORKFLOW_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: set_workflow_enabled_api
        {
            let url = BASE_URL + `/api/v1/workflows/${workflow_id}/enabled`;
            // TODO: edit the parameters of the request body.
            let body = {"enabled": "boolean"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "Workflow enabled state updated": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/gdpr/erasure/{contact_id}", () => {
        let contactId = 'TODO_EDIT_THE_CONTACT_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: erasure_contact
        {
            let url = BASE_URL + `/api/v1/gdpr/erasure/${contact_id}`;
            let request = http.post(url);

            check(request, {
                "Contact anonymized": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/supplier-conditions", () => {

        // Request No. 1: create_supplier_condition
        {
            let url = BASE_URL + `/api/v1/supplier-conditions`;
            // TODO: edit the parameters of the request body.
            let body = {"currency": "string", "deliveryTerms": "string", "earlyPaymentDiscountPercent": "string", "isDefault": "boolean", "minimumOrderValue": "string", "notes": "string", "paymentDueDays": "integer", "paymentTerms": "string", "supplierContactId": "string", "supplierName": "string", "volumeDiscountTiers": "oas_any_type_not_mapped"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/public/returns/request", () => {

        // Request No. 1: request_public_return
        {
            let url = BASE_URL + `/api/v1/public/returns/request`;
            // TODO: edit the parameters of the request body.
            let body = {"email": "string", "items": "list", "notes": "string", "orderNumber": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Return requested": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/purchase-orders/{purchase_order_id}", () => {
        let purchaseOrderId = 'TODO_EDIT_THE_PURCHASE_ORDER_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_purchase_order
        {
            let url = BASE_URL + `/api/v1/purchase-orders/${purchase_order_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_purchase_order
        {
            let url = BASE_URL + `/api/v1/purchase-orders/${purchase_order_id}`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_purchase_order
        {
            let url = BASE_URL + `/api/v1/purchase-orders/${purchase_order_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/silent-partners/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_silent_partner
        {
            let url = BASE_URL + `/api/v1/silent-partners/${id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_silent_partner
        {
            let url = BASE_URL + `/api/v1/silent-partners/${id}`;
            // TODO: edit the parameters of the request body.
            let body = {"contractDate": "date", "einlage": "string", "gewinnquotePct": "string", "gewinnvortrag": "string", "instrumentType": "instrumenttype", "kestPflichtig": "boolean", "name": "string", "notes": "string", "verlustVerrechnungskonto": "string", "verlustbeteiligung": "boolean"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_silent_partner
        {
            let url = BASE_URL + `/api/v1/silent-partners/${id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/training-assignments/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_training_assignment
        {
            let url = BASE_URL + `/api/v1/training-assignments/${id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_training_assignment
        {
            let url = BASE_URL + `/api/v1/training-assignments/${id}`;
            // TODO: edit the parameters of the request body.
            let body = {"assignedBy": "uuid", "dueDate": "date", "employeeId": "uuid", "notes": "string", "status": "assignmentstatus", "trainingId": "uuid"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_training_assignment
        {
            let url = BASE_URL + `/api/v1/training-assignments/${id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/replenishments/apply", () => {
        let sourceWarehouseId = 'TODO_EDIT_THE_SOURCEWAREHOUSEID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let targetWarehouseId = 'TODO_EDIT_THE_TARGETWAREHOUSEID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: apply_replenishments
        {
            let url = BASE_URL + `/api/v1/replenishments/apply?targetWarehouseId=${targetWarehouseId}&sourceWarehouseId=${sourceWarehouseId}`;
            let request = http.post(url);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/shipping/suitability", () => {

        // Request No. 1: shipping_suitability_api
        {
            let url = BASE_URL + `/api/v1/shipping/suitability`;
            // TODO: edit the parameters of the request body.
            let body = {"customerAnnualVolume": "integer", "items": "list", "recipient": {"city": "string", "company": "string", "country": "string", "email": "string", "name": "string", "phone": "string", "street": "string", "streetNumber": "string", "zip": "string"}, "sender": {"city": "string", "company": "string", "country": "string", "email": "string", "name": "string", "phone": "string", "street": "string", "streetNumber": "string", "zip": "string"}};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Shipping suitability results": (r) => r.status === 200
            });
        }
    });

    group("/api/pos/kasse/closing", () => {

        // Request No. 1: pos_kasse_closing
        {
            let url = BASE_URL + `/api/pos/kasse/closing`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, params);

            check(request, {
                "": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/paygap/report", () => {

        // Request No. 1: paygap_report_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/paygap/report`;
            let request = http.get(url);

            check(request, {
                "Gender pay gap report (EntgTranspG, EU 2023/970)": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/fristen", () => {
        let gewstAktiv = 'TODO_EDIT_THE_GEWST_AKTIV'; // specify value as there is no example value for this parameter in OpenAPI spec
        let bundesland = 'TODO_EDIT_THE_BUNDESLAND'; // specify value as there is no example value for this parameter in OpenAPI spec
        let voranmeldungsrhythmus = 'TODO_EDIT_THE_VORANMELDUNGSRHYTHMUS'; // specify value as there is no example value for this parameter in OpenAPI spec
        let monate = 'TODO_EDIT_THE_MONATE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let estAktiv = 'TODO_EDIT_THE_EST_AKTIV'; // specify value as there is no example value for this parameter in OpenAPI spec
        let dauerfristverlaengerung = 'TODO_EDIT_THE_DAUERFRISTVERLAENGERUNG'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: fristen_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/fristen?bundesland=${bundesland}&voranmeldungsrhythmus=${voranmeldungsrhythmus}&dauerfristverlaengerung=${dauerfristverlaengerung}&est_aktiv=${est_aktiv}&gewst_aktiv=${gewst_aktiv}&monate=${monate}`;
            let request = http.get(url);

            check(request, {
                "Steuerliche Fristen": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/marketplace/oauth/authorize", () => {

        // Request No. 1: oauth_authorize_api
        {
            let url = BASE_URL + `/api/v1/marketplace/oauth/authorize`;
            // TODO: edit the parameters of the request body.
            let body = {"config": "oas_any_type_not_mapped", "platform": "string", "redirectUri": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Authorization URL": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/coupons/", () => {
        let code = 'TODO_EDIT_THE_CODE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let discountType = 'TODO_EDIT_THE_DISCOUNT_TYPE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let isActive = 'TODO_EDIT_THE_IS_ACTIVE'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_coupons
        {
            let url = BASE_URL + `/api/v1/coupons/?page=${page}&page_size=${page_size}&is_active=${is_active}&code=${code}&discount_type=${discount_type}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/shipments", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let includeDeleted = 'TODO_EDIT_THE_INCLUDE_DELETED'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = '1'; // extracted from 'example' field defined at the parameter level of OpenAPI spec

        // Request No. 1: list_shipments
        {
            let url = BASE_URL + `/api/v1/shipments?page=${page}&page_size=${page_size}&search=${search}&include_deleted=${include_deleted}`;
            let request = http.get(url);

            check(request, {
                "Shipments found": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: create_shipment
        {
            let url = BASE_URL + `/api/v1/shipments`;
            // TODO: edit the parameters of the request body.
            let body = {"deliveredAt": "date", "labelUrl": "string", "lineItemsShipment": "oas_any_type_not_mapped", "orderId": "string", "recipientAddress": "oas_any_type_not_mapped", "shipmentDate": "date", "shippingCarrier": "string", "shippingCost": "string", "shippingMethod": "string", "signedBy": "string", "status": "string", "trackingEvents": "oas_any_type_not_mapped", "trackingNumber": "string", "trackingUrl": "string", "weightKg": "double"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Shipment created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/marketplace/platforms", () => {

        // Request No. 1: list_platforms_api
        {
            let url = BASE_URL + `/api/v1/marketplace/platforms`;
            let request = http.get(url);

            check(request, {
                "Supported platforms": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/order-confirmations/{confirmation_id}/pdf", () => {
        let confirmationId = 'TODO_EDIT_THE_CONFIRMATION_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: download_confirmation_pdf
        {
            let url = BASE_URL + `/api/v1/order-confirmations/${confirmation_id}/pdf`;
            let request = http.get(url);

            check(request, {
                "PDF file": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/employees", () => {

        // Request No. 1: create_employee
        {
            let url = BASE_URL + `/api/v1/employees`;
            // TODO: edit the parameters of the request body.
            let body = {"address": "string", "backupEmployeeId": "uuid", "bic": "string", "city": "string", "country": "countrycode", "dateOfBirth": "date", "departmentId": "uuid", "email": "string", "firstName": "string", "gender": "gender", "hireDate": "date", "hourlyCost": "string", "iban": "string", "jobTitle": "string", "lastLogin": "date", "lastName": "string", "lastUpdated": "date", "monthlySalary": "string", "phone": "string", "state": "string", "status": "employeestatus", "userId": "uuid", "weeklyHours": "string", "zip": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/quotations", () => {

        // Request No. 1: create_quotation
        {
            let url = BASE_URL + `/api/v1/quotations`;
            // TODO: edit the parameters of the request body.
            let body = {"address": "oas_any_type_not_mapped", "contactId": "string", "contactName": "string", "currency": "string", "expirationDate": "date", "files": "oas_any_type_not_mapped", "introduction": "string", "lineItems": "oas_any_type_not_mapped", "precedingSalesVoucherId": "string", "precedingSalesVoucherType": "precedingsalesvouchertype", "quotationNumber": "string", "remark": "string", "taxCondition": "string", "title": "string", "voucherDate": "date", "voucherStatus": "voucherstatus"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/auth/register", () => {

        // Request No. 1: register
        {
            let url = BASE_URL + `/auth/register`;
            // TODO: edit the parameters of the request body.
            let body = {"companyName": "string", "email": "string", "firstName": "string", "lastName": "string", "password": "string", "privacyAccepted": "boolean"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "User registered, verification email sent": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/declarations/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_declaration
        {
            let url = BASE_URL + `/api/v1/declarations/${id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_declaration
        {
            let url = BASE_URL + `/api/v1/declarations/${id}`;
            // TODO: edit the parameters of the request body.
            let body = {"declarationType": "declarationtype", "isCurrent": "boolean", "text": "string", "validFrom": "date", "version": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_declaration
        {
            let url = BASE_URL + `/api/v1/declarations/${id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/invoices/{id}/peppol", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: peppol_api
        {
            let url = BASE_URL + `/api/v1/invoices/${id}/peppol`;
            let request = http.get(url);

            check(request, {
                "Peppol BIS 3.0 Invoice XML": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/invoices/{id}/qrcode", () => {
        let reference = 'TODO_EDIT_THE_REFERENCE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let holderName = 'TODO_EDIT_THE_HOLDER_NAME'; // specify value as there is no example value for this parameter in OpenAPI spec
        let amount = 'TODO_EDIT_THE_AMOUNT'; // specify value as there is no example value for this parameter in OpenAPI spec
        let purpose = 'TODO_EDIT_THE_PURPOSE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let iban = 'TODO_EDIT_THE_IBAN'; // specify value as there is no example value for this parameter in OpenAPI spec
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let bic = 'TODO_EDIT_THE_BIC'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: generate_qrcode_api
        {
            let url = BASE_URL + `/api/v1/invoices/${id}/qrcode?iban=${iban}&holder_name=${holder_name}&bic=${bic}&amount=${amount}&reference=${reference}&purpose=${purpose}`;
            let request = http.get(url);

            check(request, {
                "QR Code for invoice payment": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/support/tickets", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let customerId = 'TODO_EDIT_THE_CUSTOMER_ID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let channelType = 'TODO_EDIT_THE_CHANNEL_TYPE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let priority = 'TODO_EDIT_THE_PRIORITY'; // specify value as there is no example value for this parameter in OpenAPI spec
        let assignedTo = 'TODO_EDIT_THE_ASSIGNED_TO'; // specify value as there is no example value for this parameter in OpenAPI spec
        let status = 'TODO_EDIT_THE_STATUS'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_tickets_api
        {
            let url = BASE_URL + `/api/v1/support/tickets?status=${status}&priority=${priority}&assigned_to=${assigned_to}&channel_type=${channel_type}&customer_id=${customer_id}&search=${search}&page=${page}&page_size=${page_size}`;
            let request = http.get(url);

            check(request, {
                "Tickets list": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: create_ticket_api
        {
            let url = BASE_URL + `/api/v1/support/tickets`;
            // TODO: edit the parameters of the request body.
            let body = {"channelId": "uuid", "channelType": "string", "customerEmail": "string", "customerId": "string", "customerName": "string", "externalId": "string", "messageBody": "string", "orderRef": "string", "subject": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Ticket created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/payroll/{id}/sv-meldungen", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: payroll_sv_meldungen
        {
            let url = BASE_URL + `/api/v1/payroll/${id}/sv-meldungen`;
            let request = http.post(url);

            check(request, {
                "": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/payments/{id}/restore", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: payment_restore
        {
            let url = BASE_URL + `/api/v1/payments/${id}/restore`;
            let request = http.post(url);

            check(request, {
                "Restored": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/compliance-trainings/", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let includeDeleted = 'TODO_EDIT_THE_INCLUDE_DELETED'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = '1'; // extracted from 'example' field defined at the parameter level of OpenAPI spec

        // Request No. 1: get_compliance_trainings
        {
            let url = BASE_URL + `/api/v1/compliance-trainings/?page=${page}&page_size=${page_size}&search=${search}&include_deleted=${include_deleted}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/posting-categories", () => {

        // Request No. 1: list_posting_categories
        {
            let url = BASE_URL + `/api/v1/posting-categories`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: create_posting_category
        {
            let url = BASE_URL + `/api/v1/posting-categories`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/participations/", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let includeDeleted = 'TODO_EDIT_THE_INCLUDE_DELETED'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = '1'; // extracted from 'example' field defined at the parameter level of OpenAPI spec

        // Request No. 1: get_participations
        {
            let url = BASE_URL + `/api/v1/participations/?page=${page}&page_size=${page_size}&search=${search}&include_deleted=${include_deleted}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/support/channels/{channel_id}", () => {
        let channelId = 'TODO_EDIT_THE_CHANNEL_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: update_channel_api
        {
            let url = BASE_URL + `/api/v1/support/channels/${channel_id}`;
            // TODO: edit the parameters of the request body.
            let body = {"config": "oas_any_type_not_mapped", "isActive": "boolean", "name": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "Channel updated": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: delete_channel_api
        {
            let url = BASE_URL + `/api/v1/support/channels/${channel_id}`;
            let request = http.del(url);

            check(request, {
                "Channel deleted": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/users/{user_id}/role", () => {
        let userId = 'TODO_EDIT_THE_USER_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: update_user_role
        {
            let url = BASE_URL + `/api/v1/users/${user_id}/role`;
            // TODO: edit the parameters of the request body.
            let body = {"role": "string", "syncPermissions": "boolean"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/institute/status", () => {

        // Request No. 1: institute_status_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/institute/status`;
            let request = http.get(url);

            check(request, {
                "Institut-Status: Fristen und Checkliste (KWG § 26, HGB § 340k/§ 341k, HGB § 325, GwG § 8)": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/payroll/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: payroll_get
        {
            let url = BASE_URL + `/api/v1/payroll/${id}`;
            let request = http.get(url);

            check(request, {
                "": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: payroll_delete
        {
            let url = BASE_URL + `/api/v1/payroll/${id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/shipments/{shipment_id}/status", () => {
        let shipmentId = 'TODO_EDIT_THE_SHIPMENT_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: update_shipment_status
        {
            let url = BASE_URL + `/api/v1/shipments/${shipment_id}/status`;
            // TODO: edit the parameters of the request body.
            let body = {"deliveredAt": "string", "signedBy": "string", "status": "string", "trackingNumber": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "Status updated": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/shipping-thresholds/deliverable", () => {
        let productId = 'TODO_EDIT_THE_PRODUCTID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let warehouseId = 'TODO_EDIT_THE_WAREHOUSEID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_deliverable
        {
            let url = BASE_URL + `/api/v1/shipping-thresholds/deliverable?productId=${productId}&warehouseId=${warehouseId}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/public/delivery-appointments/status", () => {
        let appointmentId = 'TODO_EDIT_THE_APPOINTMENTID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let email = 'TODO_EDIT_THE_EMAIL'; // specify value as there is no example value for this parameter in OpenAPI spec
        let token = 'TODO_EDIT_THE_TOKEN'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_public_delivery_appointment_status
        {
            let url = BASE_URL + `/api/v1/public/delivery-appointments/status?appointmentId=${appointmentId}&email=${email}&token=${token}`;
            let request = http.get(url);

            check(request, {
                "Appointment status": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/automations", () => {

        // Request No. 1: list_automations
        {
            let url = BASE_URL + `/api/v1/automations`;
            let request = http.get(url);

            check(request, {
                "": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/production-orders/", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGESIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let status = 'TODO_EDIT_THE_STATUS'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_production_orders
        {
            let url = BASE_URL + `/api/v1/production-orders/?page=${page}&pageSize=${pageSize}&search=${search}&status=${status}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/shipping-rules/{rule_id}", () => {
        let ruleId = 'TODO_EDIT_THE_RULE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_shipping_rule
        {
            let url = BASE_URL + `/api/v1/shipping-rules/${rule_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_shipping_rule
        {
            let url = BASE_URL + `/api/v1/shipping-rules/${rule_id}`;
            // TODO: edit the parameters of the request body.
            let body = {"carrier": "string", "country": "countrycode", "deliveryTime": "string", "isActive": "boolean", "maxWeightKg": "double", "minWeightKg": "double", "name": "string", "notes": "string", "price": "string", "priority": "integer"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_shipping_rule
        {
            let url = BASE_URL + `/api/v1/shipping-rules/${rule_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/communications/{communication_id}/restore", () => {
        let communicationId = 'TODO_EDIT_THE_COMMUNICATION_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: customercommunication_restore
        {
            let url = BASE_URL + `/api/v1/communications/${communication_id}/restore`;
            let request = http.post(url);

            check(request, {
                "Restored": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/products/", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let includeDeleted = 'TODO_EDIT_THE_INCLUDE_DELETED'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = '1'; // extracted from 'example' field defined at the parameter level of OpenAPI spec

        // Request No. 1: get_products_api
        {
            let url = BASE_URL + `/api/v1/products/?page=${page}&page_size=${page_size}&search=${search}&include_deleted=${include_deleted}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/orders", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let includeDeleted = 'TODO_EDIT_THE_INCLUDE_DELETED'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = '1'; // extracted from 'example' field defined at the parameter level of OpenAPI spec

        // Request No. 1: get_orders
        {
            let url = BASE_URL + `/api/v1/orders?page=${page}&page_size=${page_size}&search=${search}&include_deleted=${include_deleted}`;
            let request = http.get(url);

            check(request, {
                "Orders found": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/purchase-orders", () => {

        // Request No. 1: create_purchase_order
        {
            let url = BASE_URL + `/api/v1/purchase-orders`;
            // TODO: edit the parameters of the request body.
            let body = {"currency": "string", "deliveryAddress": "oas_any_type_not_mapped", "expectedDeliveryDate": "date", "lineItems": "oas_any_type_not_mapped", "notes": "string", "orderDate": "date", "poNumber": "string", "status": "purchaseorderstatus", "supplierContactId": "string", "supplierName": "string", "totalGrossAmount": "string", "totalNetAmount": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/vouchers", () => {

        // Request No. 1: create_voucher
        {
            let url = BASE_URL + `/api/v1/vouchers`;
            // TODO: edit the parameters of the request body.
            let body = {"categoryId": "string", "contactId": "string", "contactName": "string", "currency": "string", "description": "string", "fileAttachments": "oas_any_type_not_mapped", "lineItems": "oas_any_type_not_mapped", "metadata": "oas_any_type_not_mapped", "notes": "string", "openAmount": "string", "paidDate": "date", "paymentStatus": "paymentstatus", "taxAmounts": "oas_any_type_not_mapped", "taxCondition": "string", "totalGrossAmount": "string", "totalNetAmount": "string", "voucherDate": "date", "voucherNumber": "string", "voucherStatus": "voucherstatus", "voucherType": "vouchertype"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/activities", () => {

        // Request No. 1: create_activity
        {
            let url = BASE_URL + `/api/v1/activities`;
            // TODO: edit the parameters of the request body.
            let body = {"activityType": "activitytype", "assignedTo": "string", "contactId": "string", "description": "string", "dueDate": "date", "reminderDate": "date", "status": "activitystatus", "subject": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/participations/{id}", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_participation
        {
            let url = BASE_URL + `/api/v1/participations/${id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_participation
        {
            let url = BASE_URL + `/api/v1/participations/${id}`;
            // TODO: edit the parameters of the request body.
            let body = {"acquiredAt": "date", "boardAppointment": "boolean", "companyName": "string", "controlAgreement": "boolean", "legalForm": "string", "ownershipPct": "string", "purposeVehicle": "boolean", "votingMajority": "boolean"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_participation
        {
            let url = BASE_URL + `/api/v1/participations/${id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/communications/", () => {
        let contactId = 'TODO_EDIT_THE_CONTACT_ID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let channel = 'TODO_EDIT_THE_CHANNEL'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let from = 'TODO_EDIT_THE_FROM'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let to = 'TODO_EDIT_THE_TO'; // specify value as there is no example value for this parameter in OpenAPI spec
        let direction = 'TODO_EDIT_THE_DIRECTION'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_communications
        {
            let url = BASE_URL + `/api/v1/communications/?page=${page}&page_size=${page_size}&contact_id=${contact_id}&channel=${channel}&direction=${direction}&from=${from}&to=${to}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/customer-groups/{customer_group_id}", () => {
        let customerGroupId = 'TODO_EDIT_THE_CUSTOMER_GROUP_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_customer_group
        {
            let url = BASE_URL + `/api/v1/customer-groups/${customer_group_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_customer_group
        {
            let url = BASE_URL + `/api/v1/customer-groups/${customer_group_id}`;
            // TODO: edit the parameters of the request body.
            let body = {"description": "string", "memberIds": "list", "membershipFilter": "string", "name": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_customer_group
        {
            let url = BASE_URL + `/api/v1/customer-groups/${customer_group_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/down-payment-invoices/", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let includeDeleted = 'TODO_EDIT_THE_INCLUDE_DELETED'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = '1'; // extracted from 'example' field defined at the parameter level of OpenAPI spec

        // Request No. 1: list_down_payment_invoices
        {
            let url = BASE_URL + `/api/v1/down-payment-invoices/?page=${page}&page_size=${page_size}&search=${search}&include_deleted=${include_deleted}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/orders/{order_id}/tags", () => {
        let orderId = 'TODO_EDIT_THE_ORDER_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: replace_order_tags
        {
            let url = BASE_URL + `/api/v1/orders/${order_id}/tags`;
            // TODO: edit the parameters of the request body.
            let body = {"tags": "list"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "Tags replaced": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: add_order_tags
        {
            let url = BASE_URL + `/api/v1/orders/${order_id}/tags`;
            // TODO: edit the parameters of the request body.
            let body = {"tags": "list"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Tags added": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/invoices/{id}/zugferd", () => {
        let supplierName = 'TODO_EDIT_THE_SUPPLIER_NAME'; // specify value as there is no example value for this parameter in OpenAPI spec
        let supplierStreet = 'TODO_EDIT_THE_SUPPLIER_STREET'; // specify value as there is no example value for this parameter in OpenAPI spec
        let supplierCountry = 'TODO_EDIT_THE_SUPPLIER_COUNTRY'; // specify value as there is no example value for this parameter in OpenAPI spec
        let supplierCity = 'TODO_EDIT_THE_SUPPLIER_CITY'; // specify value as there is no example value for this parameter in OpenAPI spec
        let supplierVatId = 'TODO_EDIT_THE_SUPPLIER_VAT_ID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let supplierZip = 'TODO_EDIT_THE_SUPPLIER_ZIP'; // specify value as there is no example value for this parameter in OpenAPI spec
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: generate_zugferd_api
        {
            let url = BASE_URL + `/api/v1/invoices/${id}/zugferd?supplier_name=${supplier_name}&supplier_street=${supplier_street}&supplier_city=${supplier_city}&supplier_zip=${supplier_zip}&supplier_country=${supplier_country}&supplier_vat_id=${supplier_vat_id}`;
            let request = http.get(url);

            check(request, {
                "ZUGFeRD/Factur-X hybrid PDF (PDF/A-3 with embedded EN 16931 XML)": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/settings/tenant", () => {

        // Request No. 1: get_tenant_settings
        {
            let url = BASE_URL + `/api/v1/settings/tenant`;
            let request = http.get(url);

            check(request, {
                "Tenant settings": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_tenant_settings
        {
            let url = BASE_URL + `/api/v1/settings/tenant`;
            // TODO: edit the parameters of the request body.
            let body = {"companyType": "companytype", "features": {"onlineshop": "boolean", "reportBilanz": "boolean", "reportBwa": "boolean", "reportEuer": "boolean", "reportGewerbesteuer": "boolean", "reportGuv": "boolean", "reportKst": "boolean", "reportUstva": "boolean"}};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "Updated tenant settings": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/warehouses/{warehouse_id}", () => {
        let warehouseId = 'TODO_EDIT_THE_WAREHOUSE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_warehouse
        {
            let url = BASE_URL + `/api/v1/warehouses/${warehouse_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_warehouse
        {
            let url = BASE_URL + `/api/v1/warehouses/${warehouse_id}`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_warehouse
        {
            let url = BASE_URL + `/api/v1/warehouses/${warehouse_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/vouchers/{voucher_id}/restore", () => {
        let voucherId = 'TODO_EDIT_THE_VOUCHER_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: voucher_restore
        {
            let url = BASE_URL + `/api/v1/vouchers/${voucher_id}/restore`;
            let request = http.post(url);

            check(request, {
                "Restored": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/credit-notes/", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let includeDeleted = 'TODO_EDIT_THE_INCLUDE_DELETED'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = '1'; // extracted from 'example' field defined at the parameter level of OpenAPI spec

        // Request No. 1: list_credit_notes
        {
            let url = BASE_URL + `/api/v1/credit-notes/?page=${page}&page_size=${page_size}&search=${search}&include_deleted=${include_deleted}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/trainings/overview", () => {

        // Request No. 1: get_training_overview
        {
            let url = BASE_URL + `/api/v1/trainings/overview`;
            let request = http.get(url);

            check(request, {
                "HR overview of assigned trainings": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/purchase-orders/{purchase_order_id}/status", () => {
        let purchaseOrderId = 'TODO_EDIT_THE_PURCHASE_ORDER_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: update_purchase_order_status
        {
            let url = BASE_URL + `/api/v1/purchase-orders/${purchase_order_id}/status`;
            // TODO: edit the parameters of the request body.
            let body = {"status": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/paygap/export", () => {

        // Request No. 1: paygap_export_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/paygap/export`;
            let request = http.get(url);

            check(request, {
                "Gender pay gap CSV export": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/customers", () => {

        // Request No. 1: create_customer
        {
            let url = BASE_URL + `/api/v1/customers`;
            // TODO: edit the parameters of the request body.
            let body = {"address": "oas_any_type_not_mapped", "contactPerson": "string", "email": "string", "externalOrderNumber": "string", "name": "string", "paymentGracePeriodDays": "integer", "phone": "string", "vatId": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/bookkeeping/reports/kontenansicht", () => {
        let month = 'TODO_EDIT_THE_MONTH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec
        let dateTo = 'TODO_EDIT_THE_DATE_TO'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let dateFrom = 'TODO_EDIT_THE_DATE_FROM'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: kontenansicht_report_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/reports/kontenansicht?year=${year}&month=${month}&date_from=${date_from}&date_to=${date_to}&page=${page}&page_size=${page_size}`;
            let request = http.get(url);

            check(request, {
                "Account overview": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/payroll", () => {
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec
        let status = 'TODO_EDIT_THE_STATUS'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: payroll_list
        {
            let url = BASE_URL + `/api/v1/payroll?year=${year}&status=${status}`;
            let request = http.get(url);

            check(request, {
                "": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: payroll_create
        {
            let url = BASE_URL + `/api/v1/payroll`;
            // TODO: edit the parameters of the request body.
            let body = {"employeeIds": "list", "extraPayments": "list", "month": "integer", "year": "integer"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/packing/{order_number}/print-delivery-note", () => {
        let orderNumber = 'TODO_EDIT_THE_ORDER_NUMBER'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: print_delivery_note
        {
            let url = BASE_URL + `/api/v1/packing/${order_number}/print-delivery-note`;
            let request = http.post(url);

            check(request, {
                "Delivery note printed": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/emissions/entries", () => {
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: emissions_entries_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/emissions/entries?year=${year}`;
            let request = http.get(url);

            check(request, {
                "Emission entries for year": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: create_emission_entry_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/emissions/entries`;
            // TODO: edit the parameters of the request body.
            let body = {"activityValue": "string", "categoryId": "string", "description": "string", "method": "string", "scope": "string", "unit": "string", "year": "integer"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Emission entry created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/bookkeeping/jahresust", () => {
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: jahresust_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/jahresust?year=${year}`;
            let request = http.get(url);

            check(request, {
                "Jahresumsatzsteuer Ergebnis": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/warehouses/", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let isActive = 'TODO_EDIT_THE_IS_ACTIVE'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_warehouses
        {
            let url = BASE_URL + `/api/v1/warehouses/?page=${page}&page_size=${page_size}&search=${search}&is_active=${is_active}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/auth/accept-invite", () => {

        // Request No. 1: accept_invite
        {
            let url = BASE_URL + `/auth/accept-invite`;
            // TODO: edit the parameters of the request body.
            let body = {"firstName": "string", "lastName": "string", "password": "string", "privacyAccepted": "boolean", "token": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Invitation accepted": (r) => r.status === 200
            });
        }
    });

    group("/api/pos/billing", () => {

        // Request No. 1: pos_billing
        {
            let url = BASE_URL + `/api/pos/billing`;
            let request = http.get(url);

            check(request, {
                "": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/kosten-vorschau", () => {
        let month = 'TODO_EDIT_THE_MONTH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: kosten_vorschau_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/kosten-vorschau?year=${year}&month=${month}`;
            let request = http.get(url);

            check(request, {
                "Kosten-Vorschau für den Monat": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/support/tickets/{ticket_id}", () => {
        let ticketId = 'TODO_EDIT_THE_TICKET_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_ticket_api
        {
            let url = BASE_URL + `/api/v1/support/tickets/${ticket_id}`;
            let request = http.get(url);

            check(request, {
                "Ticket detail": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_ticket_api
        {
            let url = BASE_URL + `/api/v1/support/tickets/${ticket_id}`;
            // TODO: edit the parameters of the request body.
            let body = {"assignedTo": "uuid", "channelId": "uuid", "channelType": "supportchanneltype", "closedAt": "date", "createdAt": "date", "customerEmail": "string", "customerId": "string", "customerName": "string", "externalId": "string", "firstMessageAt": "date", "lastMessageAt": "date", "leadId": "uuid", "messageCount": "integer", "orderRef": "string", "priority": "ticketpriority", "resolution": "string", "status": "supportticketstatus", "subject": "string", "tags": "oas_any_type_not_mapped", "tenantId": "uuid", "updatedAt": "date"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "Ticket updated": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_ticket_api
        {
            let url = BASE_URL + `/api/v1/support/tickets/${ticket_id}`;
            let request = http.del(url);

            check(request, {
                "Ticket deleted": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/delivery-dates/", () => {
        let orderNumber = 'TODO_EDIT_THE_ORDERNUMBER'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGESIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let from = 'TODO_EDIT_THE_FROM'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let to = 'TODO_EDIT_THE_TO'; // specify value as there is no example value for this parameter in OpenAPI spec
        let status = 'TODO_EDIT_THE_STATUS'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_delivery_dates
        {
            let url = BASE_URL + `/api/v1/delivery-dates/?page=${page}&pageSize=${pageSize}&orderNumber=${orderNumber}&status=${status}&from=${from}&to=${to}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/boms/", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let productId = 'TODO_EDIT_THE_PRODUCTID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGESIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_boms
        {
            let url = BASE_URL + `/api/v1/boms/?page=${page}&pageSize=${pageSize}&search=${search}&productId=${productId}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/price-tiers/resolved", () => {
        let quantity = 'TODO_EDIT_THE_QUANTITY'; // specify value as there is no example value for this parameter in OpenAPI spec
        let productId = 'TODO_EDIT_THE_PRODUCTID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let contactId = 'TODO_EDIT_THE_CONTACTID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_resolved_price
        {
            let url = BASE_URL + `/api/v1/price-tiers/resolved?productId=${productId}&quantity=${quantity}&contactId=${contactId}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/job-applications/{application_id}/status", () => {
        let applicationId = 'TODO_EDIT_THE_APPLICATION_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: update_job_application_status
        {
            let url = BASE_URL + `/api/v1/job-applications/${application_id}/status`;
            // TODO: edit the parameters of the request body.
            let body = {"postingId": "uuid", "status": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.patch(url, JSON.stringify(body), params);

            check(request, {
                "Updated": (r) => r.status === 200
            });
        }
    });

    group("/api/pos/orders/{order_number}/print", () => {
        let orderNumber = 'TODO_EDIT_THE_ORDER_NUMBER'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: pos_order_print
        {
            let url = BASE_URL + `/api/pos/orders/${order_number}/print`;
            let request = http.get(url);

            check(request, {
                "": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/purchase-orders/{purchase_order_id}/match-invoice", () => {
        let purchaseOrderId = 'TODO_EDIT_THE_PURCHASE_ORDER_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: match_invoice
        {
            let url = BASE_URL + `/api/v1/purchase-orders/${purchase_order_id}/match-invoice`;
            // TODO: edit the parameters of the request body.
            let body = {"supplierInvoiceId": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/stock-movements/", () => {
        let productId = 'TODO_EDIT_THE_PRODUCTID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let warehouseId = 'TODO_EDIT_THE_WAREHOUSEID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let movementType = 'TODO_EDIT_THE_MOVEMENTTYPE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGESIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let from = 'TODO_EDIT_THE_FROM'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let to = 'TODO_EDIT_THE_TO'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_stock_movements
        {
            let url = BASE_URL + `/api/v1/stock-movements/?page=${page}&pageSize=${pageSize}&productId=${productId}&warehouseId=${warehouseId}&movementType=${movementType}&from=${from}&to=${to}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/coupons/{coupon_id}/restore", () => {
        let couponId = 'TODO_EDIT_THE_COUPON_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: coupon_restore
        {
            let url = BASE_URL + `/api/v1/coupons/${coupon_id}/restore`;
            let request = http.post(url);

            check(request, {
                "Restored": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/training-assignments/", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let includeDeleted = 'TODO_EDIT_THE_INCLUDE_DELETED'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = '1'; // extracted from 'example' field defined at the parameter level of OpenAPI spec

        // Request No. 1: get_training_assignments
        {
            let url = BASE_URL + `/api/v1/training-assignments/?page=${page}&page_size=${page_size}&search=${search}&include_deleted=${include_deleted}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/event-subscriptions/", () => {

        // Request No. 1: list_event_subscriptions
        {
            let url = BASE_URL + `/api/v1/event-subscriptions/`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/products/low-stock", () => {
        let threshold = 'TODO_EDIT_THE_THRESHOLD'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: list_low_stock_products_api
        {
            let url = BASE_URL + `/api/v1/products/low-stock?threshold=${threshold}`;
            let request = http.get(url);

            check(request, {
                "Low stock products": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/hebesatz", () => {
        let gemeindeschluessel = 'TODO_EDIT_THE_GEMEINDESCHLUESSEL'; // specify value as there is no example value for this parameter in OpenAPI spec
        let countryCode = 'TODO_EDIT_THE_COUNTRY_CODE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let stichtag = 'TODO_EDIT_THE_STICHTAG'; // specify value as there is no example value for this parameter in OpenAPI spec
        let name = 'TODO_EDIT_THE_NAME'; // specify value as there is no example value for this parameter in OpenAPI spec
        let plz = 'TODO_EDIT_THE_PLZ'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: hebesatz_lookup_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/hebesatz?gemeindeschluessel=${gemeindeschluessel}&plz=${plz}&name=${name}&stichtag=${stichtag}&country_code=${country_code}`;
            let request = http.get(url);

            check(request, {
                "Hebesatz Lookup": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/delivery-notes/{delivery_note_id}/pdf", () => {
        let deliveryNoteId = 'TODO_EDIT_THE_DELIVERY_NOTE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: download_delivery_note_pdf
        {
            let url = BASE_URL + `/api/v1/delivery-notes/${delivery_note_id}/pdf`;
            let request = http.get(url);

            check(request, {
                "PDF file": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/buchhalter-csv", () => {
        let dateTo = 'TODO_EDIT_THE_DATE_TO'; // specify value as there is no example value for this parameter in OpenAPI spec
        let dateFrom = 'TODO_EDIT_THE_DATE_FROM'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: buchhalter_csv_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/buchhalter-csv?date_from=${date_from}&date_to=${date_to}`;
            let request = http.get(url);

            check(request, {
                "Buchhalter CSV": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/offenlegung", () => {

        // Request No. 1: offenlegung_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/offenlegung`;
            let request = http.get(url);

            check(request, {
                "Offenlegungs-Checkliste (§ 325 HGB)": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/production-orders/{production_order_id}/costing", () => {
        let productionOrderId = 'TODO_EDIT_THE_PRODUCTION_ORDER_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: production_order_costing
        {
            let url = BASE_URL + `/api/v1/production-orders/${production_order_id}/costing`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/elster/status", () => {

        // Request No. 1: elster_status_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/elster/status`;
            let request = http.get(url);

            check(request, {
                "ELSTER integration status": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/notifications", () => {

        // Request No. 1: list_notifications
        {
            let url = BASE_URL + `/api/v1/notifications`;
            let request = http.get(url);

            check(request, {
                "Own notifications, newest first": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/payroll/summary/{year}", () => {
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: payroll_summary
        {
            let url = BASE_URL + `/api/v1/payroll/summary/${year}`;
            let request = http.get(url);

            check(request, {
                "": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/coupons", () => {

        // Request No. 1: create_coupon
        {
            let url = BASE_URL + `/api/v1/coupons`;
            // TODO: edit the parameters of the request body.
            let body = {"code": "string", "description": "string", "discountType": "discounttype", "discountValue": "string", "expiresAt": "date", "isActive": "boolean", "isCombineable": "boolean", "maxDiscountAmount": "string", "maxUses": "integer", "maxUsesPerCustomer": "integer", "minOrderAmount": "string", "productIds": "oas_any_type_not_mapped", "startsAt": "date"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/inventory-counts/{inventory_count_id}", () => {
        let inventoryCountId = 'TODO_EDIT_THE_INVENTORY_COUNT_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_inventory_count
        {
            let url = BASE_URL + `/api/v1/inventory-counts/${inventory_count_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_inventory_count
        {
            let url = BASE_URL + `/api/v1/inventory-counts/${inventory_count_id}`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_inventory_count
        {
            let url = BASE_URL + `/api/v1/inventory-counts/${inventory_count_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/recurring-templates", () => {

        // Request No. 1: create_recurring_template
        {
            let url = BASE_URL + `/api/v1/recurring-templates`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/payroll/{id}/entries/{entry_id}/pdf", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let entryId = 'TODO_EDIT_THE_ENTRY_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: payroll_entry_pdf
        {
            let url = BASE_URL + `/api/v1/payroll/${id}/entries/${entry_id}/pdf`;
            let request = http.get(url);

            check(request, {
                "PDF slip": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/customer-groups/{customer_group_id}/members", () => {
        let customerGroupId = 'TODO_EDIT_THE_CUSTOMER_GROUP_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: add_group_members
        {
            let url = BASE_URL + `/api/v1/customer-groups/${customer_group_id}/members`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, params);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/service-jobs/", () => {
        let search = 'TODO_EDIT_THE_SEARCH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let includeDeleted = 'TODO_EDIT_THE_INCLUDE_DELETED'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = '1'; // extracted from 'example' field defined at the parameter level of OpenAPI spec

        // Request No. 1: get_service_jobs
        {
            let url = BASE_URL + `/api/v1/service-jobs/?page=${page}&page_size=${page_size}&search=${search}&include_deleted=${include_deleted}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/product-categories", () => {

        // Request No. 1: list_product_categories
        {
            let url = BASE_URL + `/api/v1/product-categories`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: create_product_category
        {
            let url = BASE_URL + `/api/v1/product-categories`;
            // TODO: edit the parameters of the request body.
            let body = {"description": "string", "name": "string", "parentCategoryId": "string", "sortOrder": "integer"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/delivery-notes", () => {

        // Request No. 1: create_delivery_note
        {
            let url = BASE_URL + `/api/v1/delivery-notes`;
            // TODO: edit the parameters of the request body.
            let body = {"address": "oas_any_type_not_mapped", "contactId": "string", "contactName": "string", "currency": "string", "deliveryDate": "date", "deliveryNoteNumber": "string", "files": "oas_any_type_not_mapped", "introduction": "string", "lineItems": "oas_any_type_not_mapped", "precedingSalesVoucherId": "string", "precedingSalesVoucherType": "precedingsalesvouchertype", "remark": "string", "shippingDate": "date", "shippingMethod": "string", "title": "string", "voucherDate": "date", "voucherStatus": "voucherstatus"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/product-variants/{variant_id}", () => {
        let variantId = 'TODO_EDIT_THE_VARIANT_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_product_variant
        {
            let url = BASE_URL + `/api/v1/product-variants/${variant_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_product_variant
        {
            let url = BASE_URL + `/api/v1/product-variants/${variant_id}`;
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_product_variant
        {
            let url = BASE_URL + `/api/v1/product-variants/${variant_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/contacts/{contact_id}/timeline", () => {
        let contactId = 'TODO_EDIT_THE_CONTACT_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: contact_timeline
        {
            let url = BASE_URL + `/api/v1/contacts/${contact_id}/timeline`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/order-confirmations/{confirmation_id}/pursue", () => {
        let confirmationId = 'TODO_EDIT_THE_CONFIRMATION_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: pursue_confirmation
        {
            let url = BASE_URL + `/api/v1/order-confirmations/${confirmation_id}/pursue`;
            let request = http.post(url);

            check(request, {
                "Created DeliveryNote": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/trainings/content/{code}", () => {
        let code = 'TODO_EDIT_THE_CODE'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_training_content
        {
            let url = BASE_URL + `/api/v1/trainings/content/${code}`;
            let request = http.get(url);

            check(request, {
                "Interactive training content for the frontend player": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/contacts/{contact_id}/communications", () => {
        let contactId = 'TODO_EDIT_THE_CONTACT_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_contact_history
        {
            let url = BASE_URL + `/api/v1/contacts/${contact_id}/communications`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/products/{product_id}", () => {
        let productId = 'TODO_EDIT_THE_PRODUCT_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_product_api
        {
            let url = BASE_URL + `/api/v1/products/${product_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_product_api
        {
            let url = BASE_URL + `/api/v1/products/${product_id}`;
            // TODO: edit the parameters of the request body.
            let body = {"availability": "string", "barcode": "string", "brand": "string", "categoryId": "string", "condition": "string", "defaultLedgerAccount": "string", "defaultPrice": "string", "defaultPriceFormulaId": "uuid", "defaultTaxRate": "string", "description": "string", "gtin": "string", "height": "string", "imageLink": "string", "images": "oas_any_type_not_mapped", "isTaxable": "boolean", "length": "string", "link": "string", "maxStock": "long", "minStock": "long", "mpn": "string", "name": "string", "packageHeight": "string", "packageLength": "string", "packageWeightUnit": "string", "packageWeightValue": "string", "packageWidth": "string", "productCode": "string", "productType": "string", "purchasePrice": "string", "reorderQuantity": "long", "salePrice": "string", "shippingPrice": "string", "shippingRequiresInsurance": "boolean", "sku": "string", "stockQuantity": "long", "tags": "oas_any_type_not_mapped", "taxPrice": "string", "trackBatch": "boolean", "trackSerial": "boolean", "unit": "oas_any_type_not_mapped", "weightUnit": "string", "weightValue": "string", "width": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_product_api
        {
            let url = BASE_URL + `/api/v1/products/${product_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/warehouses/{warehouse_id}/stock/{product_id}", () => {
        let productId = 'TODO_EDIT_THE_PRODUCT_ID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let warehouseId = 'TODO_EDIT_THE_WAREHOUSE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: update_warehouse_stock
        {
            let url = BASE_URL + `/api/v1/warehouses/${warehouse_id}/stock/${product_id}`;
            // TODO: edit the parameters of the request body.
            let body = {"batchNumber": "string", "binLocation": "string", "expiryDate": "date", "productId": "uuid", "quantity": "long", "serialNumbers": "list"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: delete_warehouse_stock
        {
            let url = BASE_URL + `/api/v1/warehouses/${warehouse_id}/stock/${product_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/delivery-notes/{delivery_note_id}/pursue", () => {
        let deliveryNoteId = 'TODO_EDIT_THE_DELIVERY_NOTE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: pursue_delivery_note
        {
            let url = BASE_URL + `/api/v1/delivery-notes/${delivery_note_id}/pursue`;
            let request = http.post(url);

            check(request, {
                "Created Invoice": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/bookkeeping/bwa", () => {
        let month = 'TODO_EDIT_THE_MONTH'; // specify value as there is no example value for this parameter in OpenAPI spec
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: bwa_report_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/bwa?year=${year}&month=${month}`;
            let request = http.get(url);

            check(request, {
                "BWA Report": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/payment-gateways/oauth/callback", () => {

        // Request No. 1: oauth_callback_api
        {
            let url = BASE_URL + `/api/v1/payment-gateways/oauth/callback`;
            // TODO: edit the parameters of the request body.
            let body = {"code": "string", "gatewayType": "string", "redirectUri": "string", "state": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Gateway connected": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/payroll/{id}/email", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: payroll_email
        {
            let url = BASE_URL + `/api/v1/payroll/${id}/email`;
            let request = http.post(url);

            check(request, {
                "": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/product-variants/generate", () => {

        // Request No. 1: generate_product_variants
        {
            let url = BASE_URL + `/api/v1/product-variants/generate`;
            // TODO: edit the parameters of the request body.
            let body = {"options": "map", "priceDelta": "string", "productId": "uuid", "skuPrefix": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Created": (r) => r.status === 201
            });
        }
    });

    group("/api/v1/email-templates/{email_template_id}", () => {
        let emailTemplateId = 'TODO_EDIT_THE_EMAIL_TEMPLATE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_email_template
        {
            let url = BASE_URL + `/api/v1/email-templates/${email_template_id}`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 2: update_email_template
        {
            let url = BASE_URL + `/api/v1/email-templates/${email_template_id}`;
            // TODO: edit the parameters of the request body.
            let body = {"body": "string", "name": "string", "status": "emailtemplatestatus", "subject": "string", "variables": "oas_any_type_not_mapped"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });

            sleep(SLEEP_DURATION);
        }

        // Request No. 3: delete_email_template
        {
            let url = BASE_URL + `/api/v1/email-templates/${email_template_id}`;
            let request = http.del(url);

            check(request, {
                "No Content": (r) => r.status === 204
            });
        }
    });

    group("/api/v1/bookkeeping/datev/export", () => {
        let dateTo = 'TODO_EDIT_THE_DATE_TO'; // specify value as there is no example value for this parameter in OpenAPI spec
        let pageSize = 'TODO_EDIT_THE_PAGE_SIZE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let page = 'TODO_EDIT_THE_PAGE'; // specify value as there is no example value for this parameter in OpenAPI spec
        let accountSchema = 'TODO_EDIT_THE_ACCOUNT_SCHEMA'; // specify value as there is no example value for this parameter in OpenAPI spec
        let dateFrom = 'TODO_EDIT_THE_DATE_FROM'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: datev_export_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/datev/export?account_schema=${account_schema}&date_from=${date_from}&date_to=${date_to}&page=${page}&page_size=${page_size}`;
            let request = http.get(url);

            check(request, {
                "DATEV CSV export": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/orders/{order_id}/state", () => {
        let orderId = 'TODO_EDIT_THE_ORDER_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: update_order_state
        {
            let url = BASE_URL + `/api/v1/orders/${order_id}/state`;
            // TODO: edit the parameters of the request body.
            let body = {"sendStateToShop": "boolean", "state": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "Order state updated": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/attachments/{attachment_id}/ocr-text", () => {
        let attachmentId = 'TODO_EDIT_THE_ATTACHMENT_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: save_attachment_ocr_text
        {
            let url = BASE_URL + `/api/v1/attachments/${attachment_id}/ocr-text`;
            // TODO: edit the parameters of the request body.
            let body = {"ocrText": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.put(url, JSON.stringify(body), params);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/euer/kategorien", () => {
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: euer_kategorien_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/euer/kategorien?year=${year}`;
            let request = http.get(url);

            check(request, {
                "EÜR mit Kategorie-Aufschlüsselung": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/employees/{id}/restore", () => {
        let id = 'TODO_EDIT_THE_ID'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: employee_restore
        {
            let url = BASE_URL + `/api/v1/employees/${id}/restore`;
            let request = http.post(url);

            check(request, {
                "Restored": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/webhook-events", () => {

        // Request No. 1: list_event
        {
            let url = BASE_URL + `/api/v1/webhook-events`;
            let request = http.get(url);

            check(request, {
                "OK": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/konzern/status", () => {
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: konzern_status_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/konzern/status?year=${year}`;
            let request = http.get(url);

            check(request, {
                "Konzern-Status (§§ 290, 291, 293 HGB)": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/public/returns/status", () => {
        let orderNumber = 'TODO_EDIT_THE_ORDERNUMBER'; // specify value as there is no example value for this parameter in OpenAPI spec
        let returnOrderId = 'TODO_EDIT_THE_RETURNORDERID'; // specify value as there is no example value for this parameter in OpenAPI spec
        let returnNumber = 'TODO_EDIT_THE_RETURNNUMBER'; // specify value as there is no example value for this parameter in OpenAPI spec
        let email = 'TODO_EDIT_THE_EMAIL'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: get_public_return_status
        {
            let url = BASE_URL + `/api/v1/public/returns/status?returnNumber=${returnNumber}&returnOrderId=${returnOrderId}&email=${email}&orderNumber=${orderNumber}`;
            let request = http.get(url);

            check(request, {
                "Return status": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/bookkeeping/gewinnverwendung/export", () => {
        let year = 'TODO_EDIT_THE_YEAR'; // specify value as there is no example value for this parameter in OpenAPI spec

        // Request No. 1: gewinnverwendung_export_api
        {
            let url = BASE_URL + `/api/v1/bookkeeping/gewinnverwendung/export?year=${year}`;
            let request = http.get(url);

            check(request, {
                "Gewinnverwendungsrechnung als CSV (BOM, Semikolon)": (r) => r.status === 200
            });
        }
    });

    group("/api/v1/import/test", () => {

        // Request No. 1: test_import_connection
        {
            let url = BASE_URL + `/api/v1/import/test`;
            // TODO: edit the parameters of the request body.
            let body = {"apiKey": "string", "provider": "string"};
            let params = {
                headers: {
                    "Content-Type": "application/json", "Accept": "application/json"
                }
            };
            let request = http.post(url, JSON.stringify(body), params);

            check(request, {
                "Connection test result": (r) => r.status === 200
            });
        }
    });

}
